# fly high — Architecture Reference

**Status: normative.** This file is the live architectural contract, enforced by
[`src/render/webgpu/owners.ts`](src/render/webgpu/owners.ts) and
[`tests/architecture.boundaries.test.ts`](tests/architecture.boundaries.test.ts):
adding a second definition of an owned artifact fails `npm test` with a message
naming the owner. `RENDERING_PLAN.md` stays the programme plan; `TERRAIN_AUDIT.md`
stays the evidence; this file is what the code must satisfy *today*.

---

## 1. Single owners

Every artifact below has exactly one owner and one definition site. Consumers
import; they do not re-derive. Rows marked *planned* name the plan item that
creates the file — the ownership decision is already binding.

| Artifact | Owner | Definition site | Status |
|---|---|---|---|
| World-page payload schema — every page channel | terrain-geometry | `src/render/webgpu/world/payload.ts` | live |
| Page geometry — one number | terrain-geometry | `src/render/webgpu/world/pageGeometry.ts` | live |
| Terrain spine contract — slot geometry, atlas sizing, the season key, the node record, the parity criterion | terrain-geometry | `src/render/webgpu/terrain/TerrainSpineContract.ts` | live (`4-0`) |
| Terrain evolution contract — macro domain, height-authority ladder, erosion/hydrology exports and hydraulic geometry | terrain-geometry | `src/render/webgpu/terrain/TerrainEvolutionContract.ts` | live (`5-0`/`5-1`) |
| Shared amortised-compute meter | performance | `src/render/webgpu/core/ComputeBudget.ts` | live (`4-0b`, = `6-10` moved) |
| Guarded `ShadowDepthWrapper` factory — the one construction site | lighting | `src/render/webgpu/core/guardedShadowDepthWrapper.ts` | live (`4.5-0`) |
| Off-thread terrain material synthesis — worker, protocol and client | terrain-material | `src/workers/materialSynthesis.worker.ts`, `src/workers/materialSynthesisProtocol.ts`, `src/render/webgpu/terrain/MaterialSynthesisClient.ts` | live (`4.5-C2b`) |
| Terrain height kernel (WGSL) — the transliteration of `src/world/{seed,noise,geology,terrain}.ts` | terrain-geometry | `src/render/webgpu/terrain/TerrainKernel.ts` | live (`4-1`) |
| `TerrainPageAtlas` + residency + page generation, including heterogeneous hydrology resources | terrain-geometry | `src/render/webgpu/terrain/TerrainPageAtlas.ts` | live (`4-2`, `4-3`, extended `5-5`) |
| Terrain debug overlay — atlas residency plus macro flow/lakes/base-levels/fabric/erodibility | terrain-geometry | `src/render/webgpu/terrain/TerrainDebugOverlay.ts` | live (`4-3`, extended `5-3`) |
| Global height pyramid — the coarse field the occlusion bake marches beyond a page | terrain-geometry | `src/render/webgpu/terrain/GlobalHeightPyramid.ts` | live (`4-7`) |
| Page occlusion bake — sky visibility, bent normal, 8-azimuth horizon; and the land-cover splat bake | terrain-geometry | `src/render/webgpu/terrain/PageOcclusionBake.ts` | live (`4-7`, `4-6`) |
| CDLOD quadtree — node selection, morph, the node record, the one grid | terrain-geometry | `src/render/webgpu/terrain/TerrainQuadtree.ts` | live (`4-5`) |
| Land-cover classification — the sole authority for ground, tree species and wildlife habitat | terrain-material | `src/render/webgpu/terrain/LandCoverClassifier.ts` | live (`4-6`, `R-27`) |
| Vegetation density field (WGSL) — one shared include, never a copy | vegetation | `src/render/webgpu/detail/densityFieldWgsl.ts` | live (`4-6b`) |
| Bounded priority queue — the VEGETATION worker's queue | vegetation | `src/workers/boundedPriorityQueue.ts` | live (renamed from `terrainQueue.ts` at `4-4`) |
| Page-evolution reference producer — bounded operators, page builder, client, protocol and worker | terrain-geometry | `src/render/webgpu/terrain/TerrainErosionCompute.ts`, `src/render/webgpu/terrain/TerrainPageErosion.ts`, `src/render/webgpu/terrain/TerrainPageErosionClient.ts`, `src/workers/terrainErosionProtocol.ts`, `src/workers/terrainErosion.worker.ts` | live (`5-3`/`5-4`; CPU-worker reference) |
| Macro evolution producer and runtime — algorithm, client, protocol, worker and activation orchestration | terrain-geometry | `src/render/webgpu/terrain/TerrainMacroEvolution.ts`, `src/render/webgpu/terrain/TerrainMacroEvolutionClient.ts`, `src/render/webgpu/terrain/TerrainEvolutionRuntime.ts`, `src/workers/terrainMacroEvolutionProtocol.ts`, `src/workers/terrainMacroEvolutionRuntime.ts`, `src/workers/terrainMacroEvolution.worker.ts` | live (`5-3`; CPU-worker reference) |
| Per-page hydrology fields — flow, lake depth, soil depth and signed shore distance | terrain-geometry | `src/render/webgpu/terrain/TerrainPageHydrology.ts` | live (`5-5`) |
| Simulation terrain readback authority — L0 ring and macro fallback | simulation | `src/workers/terrainAuthority.ts` | live (`5-2`) |
| Rich terrain-sample authority adapter — evolved height/normal/slope for detail and wildlife | terrain-geometry | `src/render/webgpu/terrain/TerrainConsumerAuthority.ts` | live (`5-2` consumer closure) |
| Aerial-perspective WGSL include | lighting | `src/render/webgpu/atmosphere/AerialPerspective.ts` | live (`1C-4`) |
| Volumetric cloud shader modules — raymarch, temporal resolve, shadow | clouds | `src/render/webgpu/nature/CloudShaders.ts` | live (`2-0`) |
| Atmosphere GPU resources — transmittance LUT texture, sky-ambient LUT, blue noise, scene depth | lighting | `src/render/webgpu/atmosphere/AtmosphereGpuResources.ts` | live (`2-0a`) |
| Sky environment probe / IBL | lighting | `src/render/webgpu/atmosphere/SkyEnvironmentProbe.ts` | live (`1C-6`) |
| Celestial ephemeris — solar right ascension, moon position/phase/illuminance, the clock's Julian date | lighting | `src/render/webgpu/atmosphere/Ephemeris.ts` | live (`7-1`) |
| Star catalogue — every star drawn, the sidereal frame, star photometry | lighting | `src/render/webgpu/atmosphere/StarCatalogue.ts` | live (`7-3`) |
| Star field renderer | lighting | `src/render/webgpu/atmosphere/StarField.ts` | live (`7-3`) |
| Scotopic vision — rod/cone blend, Purkinje shift, acuity loss | lighting | `src/render/webgpu/atmosphere/ScotopicVision.ts` | live (`7-2`) |
| Quality tiers + governors | performance | `src/render/webgpu/core/QualityProfile.ts` (+ `AdaptiveGovernor.ts` `1A-6b`, `PerformanceBudget.ts` `1A-2`) | live |
| Surface material contract — the ten land-cover identities and their BRDF constants | terrain-material | `src/render/webgpu/terrain/surfaceMaterials.ts` | live (`3-0`; `4-6` inherits the enum) |
| Terrain material arrays — the ten synthesised layers, both `Texture2DArray`s | terrain-material | `src/render/webgpu/terrain/MaterialArraySynthesis.ts` (recipes, Class P) + `src/render/webgpu/terrain/MaterialArrayUpload.ts` (the GPU boundary) | live (`3-1`, split at `4.5-C2b`) |
| Terrain surface appearance — albedo, normal, roughness, AO, micro-detail, seasonal palette | terrain-material | `src/render/webgpu/terrain/TerrainSurfacePlugin.ts` | live (`3-2`; supersedes `TerrainMaterialPlugin`) |
| Runway surface painter — the analytic airport SDF, markings, rubber, ragged edge | terrain-material | `src/render/webgpu/terrain/RunwaySurface.ts` | live (`3-9`) |
| Runway earthworks profile | terrain-material | `src/render/webgpu/terrain/RunwayEarthworks.ts` | live (`3-8`; Class K — physics reads it too) |
| Vegetation density function, kilometre forest provinces and edge morphology | vegetation | `src/render/webgpu/detail/densityField.ts` | live (`1B-7`; Gate B `B-3`) |
| `MAX_TERRAIN_HEIGHT` (4,500 m) | terrain-geometry | `src/world/terrain.ts` | live (`5-A`) |
| Channel-graph extractor | water | `src/render/webgpu/water/ChannelNetwork.ts` | live (`5-9`) |
| Bathymetry clipmap | water | `src/render/webgpu/water/BathymetryClipmap.ts` | live (`5-10`) |
| Rendered-density law — stems/ha bands, falloff, woody triangle budgets | vegetation | `src/render/webgpu/detail/renderedDensity.ts` | live (`R-21`) |
| CPU texture-array mip reducer (coverage/Toksvig kernels) | performance | `src/render/webgpu/core/TextureArrayMips.ts` | live (`2-11`; `3-1` reuses) |
| Compact 32-byte instance record + its vertex-stage decoder | vegetation | `src/render/webgpu/detail/instanceFormat.ts` (+ `DetailInstanceMaterialPlugin.ts`) | live (`2-11a`) |
| Stand field — dominant mix, stand age, tint centre as a continuous field | vegetation | `src/render/webgpu/detail/standField.ts` | live (`2-11b`) |
| Foliage atlas — all 16 card layers + the layer-index map | vegetation | `src/render/webgpu/detail/FoliageAtlas.ts` | live (`2-11`; first sampler `2-12`) |
| Shared water shading helpers — fresnel, GGX assemblies, reflected sky | water | `src/render/webgpu/water/WaterShaders.ts` | live (`2-8a`) |
| `detail.worker.ts` | vegetation | `src/workers/detail.worker.ts` | live (`1B-10`) |
| Aircraft form, materials and cockpit presentation | aircraft | `src/render/webgpu/aircraft/` | live (Gate `A-1`–`A-4`) |
| Land-cover classification — the one authority for terrain splat, vegetation species and wildlife habitat | terrain-material | `src/render/webgpu/terrain/LandCoverClassifier.ts` | planned `4-6` |

Phase 0 added four more contracts under the same enforcement: the environment
clock (`src/world/environmentClock.ts`), the simulation terrain authority
(`src/sim/terrainGrid.ts`), the terrain collision mirror
(`src/render/webgpu/terrain/TerrainCollisionMirror.ts`), and the shared
receiver registry (`src/render/webgpu/core/SharedReceiverRegistry.ts`).

**The channel rule.** Every addition of a page channel goes through one PR
against `payload.ts`. No page-channel type is declared anywhere else.

**The tier rule.** Subsystems contribute *data fields* to
`WebGpuQualityProfile`; they do not branch on `profile.tier` with their own
constant tables. Three pre-Phase-0 readers are grandfathered in the boundary
test; the list only shrinks.

## 2. Page geometry — one number

- Base page extent: **512 m** (level `n` extent = `512 · 2ⁿ`)
- Gutter: **4 samples**, every page kind, extending *outside* the page
- Height core: **256** (stored edge 264)
- Every other channel core: **128** (stored edge 136)
- Core sample `(row, column)` lives at stored index
  `(row + gutter) · storedEdge + (column + gutter)`; the world coordinate of
  core sample `i` is `pageOrigin + i · texelSize`.

No 132², no 260², no 66². The same addressing convention serves Phase 1's tile
halo (`gutter = 1`) and Phase 4's atlas (`gutter = 4`).

## 3. The physics/render consistency invariant (§1.3)

**The surface the aircraft touches and the surface on screen are produced by
the same authority.** Explicit `worldEvolution: "analytic"` worlds preserve the
historical pointwise kernel. The default `"eroded"` world renders completed
evolution pages and serves physics from the fixed ladder **L0 readback → macro
grid → analytic recovery**. A graphics tier never changes that content choice.

- Every physics terrain query routes through `src/sim/terrainGrid.ts`. The
  boundary test forbids direct collision-kernel imports anywhere else. The
  worker-side implementation is `src/workers/terrainAuthority.ts`: complete L0
  Catmull-Rom stencils win, the cell-centred macro grid is the deterministic
  fallback, and an incomplete L0 edge is never clamped into a false hit.
- The renderer publishes the macro grid once and transfers completed L0 page
  cores only after atlas upload and collision readback. Simulation owns the
  counters. `RenderDiagnostics.collisionSamplesServedByFallback` reports the
  worker's `analyticServed` count; it is not a render-side proxy counter.
- `TerrainConsumerAuthority.ts` is the only adapter from that height ladder to
  a rich `TerrainSample`. Detail and wildlife retain the analytic climate and
  material fields, but derive height, normal and slope from the evolved
  authority (and preserve authored airport earthworks exactly). Eroded-page
  hydrology is built before erosion scratch disposal, transferred with the
  final page, uploaded to all four heterogeneous channel resources, and only
  then atomically marked resident and published to this consumer authority.
- The invariant tests live in `tests/sim.terrain-authority.test.ts` and
  `tests/sim.terrain-authority-ladder.test.ts`: runway
  influence exactly 1.0 across the apron; ground clearance never negative over
  a real-terrain profile; render-path heights equal physics-path heights at L0
  (exact at f32 storage precision); the crash-recovery ring fully served by the
  active authority; and incomplete page stencils fall through legally.

> **Amended 2026-08-19 by `3-8`.** They no longer pass trivially. The runway
> earthworks gave the apron a 0.35 m camber, and `sampleTerrainCollision`'s
> runway branch returned before any height sampling — so the aircraft would
> have touched down on a plane up to 0.35 m from the surface on screen, worst
> at the edges where a crosswind landing puts you. The four tests above would
> **not** have caught it: they assert `getAirportInfluence == 1.0`, which stays
> true. The influence was fine; the height behind it was what changed.
>
> The fix is structural rather than local: the profile is a named function
> (`terrain/RunwayEarthworks.ts`) and BOTH authorities call it — the renderer
> through `sampleFilteredTerrainHeight`, physics through
> `sampleTerrainCollisionHeight`'s fast path, which still evaluates one
> analytic expression with no noise. Two invariants joined the suite:
>
> - **63** — collision height inside the apron equals the rendered earthworks
>   profile to within 1 mm, with a non-vacuity check that the camber is
>   actually being applied.
> - **64** — the 0.5 m `|natural − final|` contour is not a closed convex
>   curve, disproved constructively by two points inside whose midpoint is
>   outside. A convex contour means the profile is still a disc.
>
> The contact normal follows the camber too: a flat normal on a cambered
> surface is the same lie one derivative up.

## 4. Season and time of day (§1.6)

The rendering inputs are two continuous scalars — `EnvironmentClock
{ dayOfYear, solarTimeHours }` (`src/world/environmentClock.ts`) plus
`WorldDefinition.latitudeDegrees` (default 45°N). `TimeOfDayPreset` survives
only as a UI label; after `1C-1` the renderer never branches on it.

**The threading rule:** `dayOfYear` is part of every seasonal field function's
signature *from the moment it is first written* — the vegetation density field
(`1B-7`), the appearance field (`2-18`), the surface palette (`3-10`), the
land-cover classifier (`4-6`). The boundary test checks each of these files
for an environment-clock reference as it comes into existence.

**Amended 2026-08-19 (`3-10`).** The surface palette landed and left the
planned marker behind: `surfaceSeasonalResponse(spec, dayOfYear,
latitudeDegrees)` lives in `terrain/TerrainSurfacePlugin.ts` and the boundary
test now enforces the rule on it rather than deferring. It is **anchored**, the
way every `R-13` term is: the raw seasonal curve is divided by its own value at
`TERRAIN_REFERENCE_DAY_OF_YEAR`, so at the midsummer default clock the tint is
exactly (1, 1, 1), the roughness delta is exactly 0, and the shipped world is
untouched. The same anchoring governs the fragment snow blanket, which is
driven by the snowline's DESCENT below its reference altitude and therefore
contributes nothing at the reference day — the classifier already owns the
base snowline, and a second unanchored blanket at the same altitude drew
closed white iso-contours around every mountain in the first capture.

**Amended 2026-08-19 (Gate 7A).** The clock carries no YEAR, and the moon's
phase repeats every 29.53 days rather than every 365 — so `Ephemeris.ts`
anchors it to a named reference epoch (`EPHEMERIS_REFERENCE_JULIAN_DAY`,
2026-01-01). Moon phase is then a deterministic function of `dayOfYear`,
which is what `7-1` asks for, and a pinned capture clock produces a pinned
moon. Two solar models now exist by necessity: `EnvironmentDirector` keeps
owning the sun's rendered DIRECTION (the NOAA elevation/azimuth every
lighting consumer reads) and `Ephemeris` owns the equatorial position the
sidereal frame needs. They are held to agreement by test — declination within
0.4°, the low-precision series' own error — rather than by hope.

**Amended 2026-08-18 (`PRE_PHASE_4_REALIGNMENT.md` §4, `R-13`).** The rule had a
hole: the check is *syntactic*, and the one file that most needed the clock was not
in the family. `src/world/terrain.ts` carries `classifyBiome` — which decides snow
at `temperature < 0.2 || height > seaLevel + 1_520` — and §5 makes it the
source `4-1` transliterates into WGSL, so `4-6`'s seasonal classifier would have
been transliterated from a kernel with no clock: exactly the retrofit this rule
exists to prevent. **Landed per `R-13` (recorded 2026-08-19):**
`seasonalTemperatureOffsetK(dayOfYear, latitudeDegrees)` is a pure kernel function
in `src/world/terrain.ts`, and the file joined `SEASONAL_FIELD_FAMILY` in the same
commit — deliberately *without* threading the offset into
`sampleTerrainTemperature`/`classifyBiome`, which stay climatic (the `R-13`
decision-log row below: snow is appearance, not classification).
`EnvironmentDirector` fills `snowCoverage` from the same kernel; `surfaceWetness`
and `precipitation` remain deliberately 0 until a precipitation model owns them
(the R-16 recorded decision — nothing renders precipitation in Phases 2–5; the
`2-1` weather bake carries the channel for that future consumer).

## 5. Kernel portability (0-4)

`src/world/{seed,noise,terrain,geology}.ts` is simultaneously the physics
authority (until `5-2`) and the source `4-1` transliterates into WGSL:

- `unitFloatFromHash` returns a **24-bit** quotient — exactly representable in
  f32 on both sides.
- Every noise lattice is periodic with `NOISE_LATTICE_WRAP_PERIOD_CELLS`
  (2¹⁷ cells); coordinates are wrap-reduced in f64 before any floor. The wrap
  is an exact no-op within ±65,536 cells of the origin (≥ 2.8×10⁶ m at the
  finest octave).
- `pow` bases are explicitly `max(0, …)` so `4-1` is a transliteration.
- `filterWidthMeters` is a **required positional parameter** of
  `sampleNaturalTerrainHeight`, `sampleGeologicalRelief`,
  `sampleTerrainMoisture`, `sampleTerrainTemperature` — a behavioural no-op
  (`0` everywhere) until `1B-2` lands band-limiting. Collision keeps `0`
  forever.

## 6. Test environments (0-8)

Two Vitest projects: `vitest.config.ts` (Node — every pure assertion, runs in
seconds, part of `npm run verify`) and `vitest.gpu.config.ts`
(`npm run test:gpu` — headless Chromium via Playwright with
`--enable-unsafe-webgpu --use-angle=metal`, full-Chromium `channel:
"chromium"`; the headless shell has no GPU). GPU tests live in `tests/gpu/`
and are excluded from the Node project. Run `test:gpu` explicitly and at every
gate boundary.

Neither project is a portability oracle: both compile through whatever Tint
build ships with the local Chromium, and Tint accepts WGSL the specification
rejects. Where a construct is known to differ, assert it **statically over the
sources** in the Node project instead (assertion 51b is the standing example).

---

## Decision log

| Item | Decision | Detail |
|---|---|---|
| Gate A (`A-1`–`A-3`) | Aircraft owns its loft/airfoil geometry and its small deterministic surface recipes; Gate A reuses the shipped CPU mip/Toksvig reducer rather than inventing a second synthesis pipeline. | Phase 3's implementation deliberately changed the plan's GPU synthesis sketch to CPU generation plus explicit mip upload, so “on the `3-1` pipeline” means reuse the live `TextureArrayMips` reduction contract, not resurrect the superseded compute design. Trainer and jet paint recipes each synthesize 64² albedo, tangent-normal and packed material maps with panels, rivets, seams, filler, soot, wear and livery; ordinary PBR maps preserve the shared aerial/cloud receiver plugins. Babylon 9.21.2 needs the cloud receiver's explicit sentinel define so later aerial-plugin attachment cannot alias cloud+aerial and aerial-only effect-cache entries. Worst live aircraft surface allocation is about 0.188 MiB. Clearcoat/transmission glass reads the existing sky IBL; cockpit instruments stay ordinary aircraft-owned meshes. |
| Gate A (`A-4`) | Cockpit occlusion is a camera-layer decision; propeller blur is a continuous material crossfade, never mesh visibility state. | Bit 27 is reserved for cockpit-obscuring exterior skin. Cockpit cameras clear it and chase cameras include it; shadow casters remain visible in both modes. Trainer blades remain solid through 15 rad/s and crossfade to a radially blurred disc by 35 rad/s. Hub, blades and disc never strobe through `setEnabled`, and the disc alone opts out of shadow casting. |
| Gate A (`A-5`) | Wildlife recognition changes prototype geometry/material character, not the population or batching architecture. | Gull, hawk, deer and boar keep the ten fixed shared thin-instance batches, generation/AI/LOD/species rules and shadow metadata. Right-handed winding, signed volume and outward normals are structural contracts after the first review caught inward-facing shells. Feather, fur and keratin materials compile on the adapter. Booked steady allocation is 37,716 B prototype geometry plus the unchanged 1,310,720 B fixed matrix buffers (1.286 MiB total). |
| Gate A (capture) | Exactly one Gate-A-close visual rebaseline is authorised; performance floors are not. | The binding Gate A text omitted a churn policy even though the rebuilt aircraft occupies persistent pixels. Its implementation record therefore authorises one close capture after Gate B's sanctioned vegetation capture. It may accept intended aircraft pixels only; it cannot disguise unrelated drift or lower fps/hitch gates. |
| Gate B (`B-0`) | Never infer per-frame present wait from an asynchronous GPU aggregate with no frame id. | A start-to-start interval is paired with the preceding frame's CPU duration. Babylon's WebGPU counter remains an independent distribution because it does not identify the submitted frame; current and p95 present residuals remain null until correlation exists. `frameIntervalP95Ms` is the honest new diagnostic. The committed pre-gate nine-shot envelope was 17.3–33.6 ms after the slower of overlapping CPU/GPU p95s, explicitly labelled an aggregate inference rather than a timer. |
| Gate B (`B-1`) | Presentation time is simulation-time plus a smoothed worker-clock offset, with one-snapshot latency and at most 50 ms prediction. | Arrival jitter no longer re-anchors the render clock. A 0.1 EMA estimates main-minus-sim time; sampled time is monotone and resets with the worker epoch. Prediction uses velocity and the simulation's body-rate quaternion convention. Exterior position, aim, up and FOV share one exponential response; cockpit is exact and camera cuts snap. |
| Gate B (`B-4`) | Origin-stale detail stays correct through both mesh and pre-world shader compensation. | Every batch records its build origin and immediately translates by `builtOrigin − currentOrigin` while the amortised rebuild proceeds. Band culling and impostor facing execute before Babylon's world transform, so the same per-submesh offset is bound as a material uniform. NullEngine and real-adapter tests cover mixed-origin batches sharing one material. |
| Gate B (`B-2`) | The crown/trunk draw merge is **rejected**; the opaque trunk pre-fill remains architectural state. | The attractive model (347 → 186 draws) lost on the adapter: all five core sub-30 shots regressed 0.78–2.09 ms GPU, and only one of all nine cleared +2 ms. The merged alpha-test path, packed ratio lane and proposed ceiling re-pin were reverted. Existing `VEGETATION_DRAW_CEILING` and `VEGETATION_FRAME_DEBT_RATIO` continue to describe the split runtime and open debt. |
| Gate B (`B-3`) | Forest extent, openings, hard disturbances and edge morphology have one owner: `densityField.ts`. | A 7.2 × 5.4 km gate authors whole meadow/forest provinces; the 260 m glade reaches a 0.02 floor; succession has full amplitude; thresholded windthrow supplies a hard edge. `forestEdge` shortens and broadens generated stems without changing the stand/species decision R-27 reserves for Phase 4. Tests select named closed-forest windows rather than assuming world zero is forest. |
| `3-0` | Ten material identities, ordered as the **ecotone axis**; tiling periods are distinct primes in decimetres. | `3-2`'s provisional splat interpolates the material id across a triangle and brackets the integers it lands between, so indices 0–5 are the chain of biome primaries in climatic order (sand → grass → forest floor → shrub → rock → snow) and every pair of biomes that can share an edge is one step apart. Measured, not asserted: the first ordering put sand between shrub and rock and the `approach-500ft` capture came back with bright closed rings around every mountain (sand 0.42 albedo against shrub 0.075). The plan's eight published periods are not mutually prime — 3.7/7.4 is an exact 2:1 — so every period is now a distinct prime number of decimetres and the closest pair realigns at 66.7 m (assertion 52). |
| `3-0` | `SHADOW_DEPTH_BYTES` corrected 5 → 4. | Material arrays pushed tiers 2 and 3 past their ceilings, and the row was checked before any feature was cut. `DepthOnlyCascadedShadowGenerator` calls `createDepthStencilTexture(comparison, true)` and stops, so `renderTargetTexture.pure.js:517`'s defaults apply — no stencil, format 14 (`DEPTH32_FLOAT`) — and `webgpuTextureManager.js:279` maps that to WebGPU `depth32float`. 64 MiB of phantom allocation at tier 2. |
| `3-1` | CPU synthesis, not GPU compute (deviating from `C2`'s split). | `updateMipLevel` uploads CPU bytes, so a GPU mip 0 must be read back before the Toksvig reduction can run — a full readback costing more than the synthesis. Measured 1.07 s for all ten 512² layers, once, on seed change, on no frame budget. It also makes assertions 53/54/55 ordinary Node tests. Ultra's array edge drops 1024 → 512 for the same reason: ~4.3 s of blocked main thread at startup, for a resolution the de-tiling warp and 16× anisotropy largely mask. Both reopen if synthesis moves to GPU compute. |
| `3-1` | Toksvig `k = 0.5`; each level reduces the level EMITTED above it. | At `k = 1` a mip whose normals average to \|avgN\| = 0.9 takes rough 0.5 → 0.59, which over-mattes mid-range rock; 0.5 gives 0.55. Measured against a plain box chain at mip 3: every layer comes out matter, rock by +7.5 and gravel by +11 bytes (assertion 54's non-tautology check). Normal strength is a declared RMS slope per material rather than a relief-in-metres: the physical derivation left the normals so nearly parallel that the whole Toksvig term was worth 0.2–0.6 of a roughness byte at mip 3 — the plan's "single most important anti-plastic measure" doing nothing. |
| `3-1` | Albedo is stored as `sqrt(linear)` (gamma 2.0), squared on read; every layer is high-passed against its own local mean, keeping 28%. | Linear RGBA8 gave forest floor a mean albedo byte of 15 and asphalt 11 — a dozen usable levels for the two materials that cover most of the world. And a layer that still carries metre-scale energy at mip 6 shows its whole tiling period as a regular quilt: measured at ~1 km on `approach-500ft`, a 4.3 m sand tile repeating every four screen pixels. The de-tiling warp cannot help there (its finest scale is 28 m), so the energy is removed at synthesis and the hundred-metre and kilometre variation is put back by `3-4`'s macro term in the shader, where it does not repeat. |
| `3-2` | Regex anchors, recorded verbatim (minified, unversioned, shipped WGSL — they WILL change on a Babylon bump). | Roughness: `var roughness: f32=reflectivityOut.roughness;var diffuseRoughness: f32=reflectivityOut.diffuseRoughness;` (`pbr.fragment.js:240`). AO: the `);` closing `aoOut=ambientOcclusionBlock(` at ~174 — **not** the plan's `:245`, which sits inside `#if defined(METALLICWORKFLOW) && defined(REFLECTIVITY) && defined(AOSTOREINMETALMAPRED)` and never compiles for a material that binds no reflectivity texture. F0: `var specularEnvironmentR0: vec3f=reflectivityOut.colorReflectanceF0;` (`pbrBlockReflectance0`). Assertion 57 reads the PROCESSED effect source on a real device; its Node sibling matches all three against the shipped Babylon files, so a bump fails `npm test` too. |
| `3-4` | De-tiling warp 1.6 (the phase's first tuning knob). | Three scales at 2048 m, 176 m rotated 13.7° and 28 m rotated 61.2° — **not** the deleted build's 36.3°, which is within 1.3° of the 35° geological fabric the audit measures at 23.6:1 anisotropy. At 1.0 the far ground still carried a visible repeat on `approach-500ft`; 1.6 warps by up to ~58 m with a worst local stretch of ~16%. |
| `3-5` | Triplanar per tier: planar / biplanar / triplanar / triplanar. | §5.3's row. Every band-based recipe (rock joints, sand ripples, concrete float sweeps, snow sastrugi) had to be re-authored to CURVE its lines: a perfectly periodic sub-metre lattice moirés into a metre-scale quilt at grazing angles, which is what the first capture showed over every flat surface. |
| `3-6` | Transition depth `d = mix(0.06, 0.5, saturate(fp / 3))` (the phase's second tuning knob). | Partition of unity checked over 20,000 randomised inputs (assertion 60), including the degenerate all-equal case. |
| `3-7` | Wetness is wired to a constant zero until `6-5` — **except** submerged ground, which is unambiguous now. | Without it the seabed showed through every lake as dry land: the WATER biome's primary must be sand (beach is its only ecotone neighbour) and dry sand is the brightest material in the table. Wetting plus a silt-and-water-column tint lands near the 0.08 the retired water palette used. |
| `3-8` | Camber 0.35 m, measured DOWNWARD from the centreline; three-zone cut/fill with an inward-only noise-modulated blend distance. | The centreline keeps `airport.elevation` exactly, because that is the aircraft's spawn datum and the height every pre-Phase-3 test was written against; crowning upward would have raised the whole runway by 0.35 m relative to spawn. Chord error 5.8 mm at 8 m vertex spacing, so no special tessellation under the airport. The blend distance is modulated *inward only* so `airport.terrainBlendDistance` stays a hard outer bound and `getAirportInfluence == 0` still implies untouched terrain. |
| `3-8` | Cut blends into fill over 4 m of height difference; it is not branched on. | Found by adversarial review of the diff, not by a test. Cut and fill differ by more than their batter grade — the bench term is `+bench` for one and `−bench` for the other — so `if (natural < platform)` put a `2 × bench` step on the closed contour where the two meet: a **ring of 1.09 m cliffs around the airport**, measured over 0.25 m of ground on three seeds, in the collision surface as much as the rendered one. Pinned now by a relative continuity invariant: the earthworks may not step more than 1.6× what the natural surface steps over the same walk. |
| `3-9` | The apron slab is deleted and not replaced. | The plan lists it among the deletions and asks for no successor. It sat 115 m across from the centreline, 84 m outside the graded platform, on natural terrain that slopes — painting concrete there would be wrong. Hangars stay (§1.5: the only scale reference on final approach); `7-10` replaces them properly, apron included. |
| `3-1` | The material arrays are synthesised **one material per frame, from the frame loop** — never inside `FlightRenderer.create()`, and never from a timer. | ~1 s of unbroken main-thread synthesis in the clipmap constructor killed the first frame: a foliage draw reached `createBindGroup` with an entirely unbound material context. The same arrays at an 8×8 edge start cleanly, so it is the stall and not the textures. `update()` runs only from the `world-page-visibility` pass, which runs only inside `render()`, which happens only once `create()` has resolved — so the frame loop is both the "startup is over" signal and the pacing. A `setTimeout` chain is NOT equivalent: it tripped the volumetric cloud system's 15 s pipeline barrier, and under the capture harness's headless Chromium it never completed at all. When the arrays land, every resident page must `resetDrawCache()` — a page already drawn holds its own draw wrapper and render bundle against the pre-plugin effect and keeps using it. |
| `51b` | No WGSL in this repo may target a multi-component swizzle with a compound assignment; a Node test scans the sources for it. | A swizzle is not a *reference* in WGSL, so `history.rgb *= s` is invalid — but the Tint build behind the GPU suite's Playwright Chromium accepts it, and one shipped in Phase 2's cloud reprojection. Spec-strict validators reject the module, the pipeline never compiles, and the app hangs on "PREPARING AIRSPACE" with 21 GPU tests still green. The adapter in front of us cannot be the portability oracle; the static scan can. |
| `R-26` | The light rig's ground bounce is `skyHorizon × meanSurfaceAlbedo × 1.15`; the SH below-horizon attenuation floors at that albedo rather than at 0.25. | Retires `D-6` and `D-9`, both ground-bounce fakes tuned against a ground colour Phase 3 replaced. The calibration constant is chosen so the reference day+clear key reproduces the retired palette row to within 0.03 on red and green; the blue it loses is the part that was never physical — ground bounce cannot be bluer than the ground. Winter now bounces >1.5× summer, which a constant floor could not say. |
| `0-9` | **Premise validated** — plugin vertex displacement + `ShadowDepthWrapper` compose on WebGPU/WGSL. Terrain stays a `PBRMaterial` with plugins; no `ShaderMaterial` re-plan. | Working incantation, verbatim: attach every vertex-participating plugin to the `PBRMaterial`, then `material.shadowDepthWrapper = new ShadowDepthWrapper(material, scene)` **before the material's first effect compiles** (the wrapper only learns about base-material effects through `onEffectCreatedObservable`; attached later it silently falls back to the undisplaced default depth pass). No `remappedVariables` needed. Proven in `tests/gpu/shadow-depth-wrapper.test.ts`: without the wrapper the shadow ignores a 25 m plugin displacement; with it, the shadow tracks within measurement noise, composed with `CloudShadowMaterialPlugin` on the same material. |
| `0-8` | Vitest browser mode + Playwright (not the manual shader-check page). | Chromium (full, new-headless) acquires a WebGPU adapter and Babylon `ComputeShader` compiles, dispatches and reads back in ~2 s. Provider: `@vitest/browser-playwright`. Harness gotcha worth its own line: manual `scene.render()` must be wrapped in `engine.beginFrame()/endFrame()` or nothing is submitted, and canvas snapshots must be taken synchronously with the submit. |
| `0-3` | Streaming options: module defaults + `levelPenaltyMeters: 400`. | The module default of 0 assumes explicit parent biasing, which the CPU tile path does not do; 400 m/level preserves the deleted `distance + level·400` fine-first ordering at equal corridor cost. Observed change: ahead-of-aircraft pages now rank before behind pages at equal distance (pinned in `tests/render.webgpu-terrain-clipmap.test.ts`); a ring-count-only profile change no longer regenerates identical pages (the deleted generation counter used to re-request everything). |
| `0-3` | Lifecycle states exercised by the CPU path: `unloaded → queued → loading → cpu-ready → uploading → resident` (synchronous upload) and `resident → evicting → unloaded`; failure path `loading → failed → queued`. | The asynchronous upload half was untested until `4-2`. **Closed at `4-2`:** a GPU-generated page runs `queued → generating → resident` and becomes resident only when its bounds READBACK resolves, which is real GPU latency; `cancelEviction()`, `cancelOperation(token, true)` and an epoch-rejected completion all have named tests. The `generating` branch is a SECOND path, not a widening — `beginUpload` still asserts `cpu-ready`, which is the check that catches an upload issued before its payload exists. |
| `0-6` | `timeOfDay` label → clock migration: `dawn → (171, 5.5)`, `day → (171, 12.5)`, `golden → (171, 19)`. | All three sit on the same midsummer day at the default 45°N — the presets were three times of one pleasant flying day. Exported as `TIME_OF_DAY_PRESET_CLOCKS`; `1C-9`'s preset buttons write these same pairs. |
| `0-4` | Golden values re-verified rather than re-baselined. | The 24-bit truncation moves every lattice value by < 1.2×10⁻⁷ (≲ 0.2 mm of terrain), and the domain wrap is an exact no-op inside ±2.8×10⁶ m. Every world threshold test, the 384-seed airport audit, and `sim.flight.test.ts` pass unchanged — the latter checked in isolation from any rendering change, per R-0E. |
| `1A-4` step 3 | Double-blend hypothesis **refuted by measurement**; culling enabled anyway. | `tests/gpu/cloud-shell-culling.test.ts`: a premultiplied 0.5-alpha camera-centered BACKSIDE shell reads identically to the idempotent alpha-1 control — one blend per pixel, cull on or off (geometrically, a ray from the shell's center crosses the surface once; the second hemisphere is clipped behind the camera). `backFaceCulling = true` is therefore visually a no-op today, kept as protection if the shell ever de-centers; Babylon's render-target winding flip and frontFace inversion cancel, so BACKSIDE stays correct. No `densityMultiplier`/extinction re-tune needed. |
| `1A-6a` | Per-tier pixel caps: 1.0 / 1.5 / 2.4 Mpx; DPR ceilings 1 / 1.5 / 2. | Default tier renders ≤ 1.5 Mpx on the reference 1512×982 @ DPR 2 viewport (was 5.94 Mpx). Ultra's 4.0 Mpx row arrives with the four-tier table at `1A-6b`. |
| `1A-5` | Depth-only CSM via `noColorAttachment` override, not `useRedTextureType: false`. | The plan's premise was stale: Babylon 9.21.2's CSM already defaults `useRedTextureType = true` (R16F), so the saving left was the never-sampled colour attachment itself. `DepthOnlyCascadedShadowGenerator` overrides `_createTargetRenderTexture` to drop it; PCF keeps sampling the depth texture (proven on-adapter in `tests/gpu/depth-only-csm.test.ts`). |
| `1B-3` | Ladder: constant resolution 65 at tiers 1–3 (33 at tier 0). | Constant per-tier resolution keeps every adjacent-level ground-sample ratio exactly 2:1, killing the audit's 4:1 L2/L3 T-junction. Terrain page generation p95 measured 7.2–7.5 ms against the 9 ms budget (M-series, `perf:capture` report). |
| `1B-11` | MSAA via `toneMap.samples` (the first post-process owns the offscreen beauty target), not `DefaultRenderingPipeline`. | The tone-map post-process already forces the offscreen target, so its `samples` property is the exact place MSAA lives; a full pipeline import would drag unrelated passes. FXAA detaches whenever MSAA > 1 — running both softens the image. Tier 2 runs 2× (not 4×): the 700 MiB budget assertion caught a 734 MiB estimate with 4× alongside the full-distance 4096² CSM; `4-8`'s near-field maps buy 4× back. |
| `1B-9` | Domain-warp noise deleted from the scatter; stems interpolate density bilinearly. | The warp's own lattice re-introduced a 37 m spectral line — the exact artefact class `1B-9` exists to kill. Blue-noise scatter is a global 32 m block lattice, stratified full-cell jitter (exact zero line spectrum), O(n) rank thinning over a spatial hash, per-subcell seed streams for cross-page determinism. |
| `1B-7`/`1B-9` | Ecological density is authored; the renderer thins by rendered share. | The ecological field's closed-forest density is unrenderable as instances (measured ~39 M triangles, ~190 ms CPU per page set). Selection-keyed rendered-share thinning (near budget 40 + 30·vegetationDensity stems/ha, (1000/d)² distance falloff, floor 0.04) keeps the field's statistics testable while the renderer draws a budgeted sample. |
| `1B-13` | fp16 FFT with per-axis 1/N normalisation on the last stage of each axis. | Normalising once at the end overflows half floats mid-transform; splitting 1/N per axis keeps every intermediate in fp16 range. Cascade-energy parity vs fp32 asserted per cascade on the measured most-energetic cascade (a fixed "largest" cascade is nearly empty at low wind). |
| `1C-2` | One exposure: `1.08 × (E_ref/E)^0.12`, `REFERENCE_EV100 = 15.27`. | The reference key anchors at the old day preset's sun height (sin 0.82), so day+clear is preserved exactly and any look change is a detected bug. Adaptation is deliberately weak (k = 0.12) — dawn stays dim; the clamp [0.3, 2.6] is `1C-10`'s twilight floor. Assertion 29 greps `src/` so no shader multiplies a private exposure again. |
| `1C-3` | Transmittance/multiple-scattering LUTs are CPU-baked (256×64 and 32×32), not GPU passes. | The bake is microseconds at these sizes, runs in Node for tests, and the CPU-side model must exist anyway as the TS mirror for exposure, IBL and the agreement tests. The GPU never samples the transmittance LUT in Phase 1 — see the `1C-4` sun-transmittance decision. |
| `1C-4` | Turbidity expressed once: `mieTurbidityMultiplier = 1 + humidity·26` (clear = 12.7×). | Textbook Mie coefficients leave ~44% transmittance at 45 km — real mountains stay visible — failing the plan's own ≥95%-opacity exit criterion. With the multiplier, clear-weather luminance transmittance at the 45 km far plane measures ≈4.6% (τ_green ≈ 3.1) while a 10 km ridge keeps ≥40% — haze, not soup. Pinned in `tests/render.webgpu-aerial-perspective.test.ts`. |
| `1C-4` | Sun transmittance rides as a per-frame uniform (camera altitude), not a per-fragment LUT fetch. | One fewer sampler in every consumer (PBR plugin UBO, three ShaderMaterials, the sky), exact TS/WGSL agreement by construction, and the error — sun transmittance varying along the path — is second-order at ≤45 km path lengths. Assertion 31 measures TS vs GPU agreement within 1% over 100 probes × 2 atmospheres on a real adapter. |
| `1C-4` | `terrainRings` 6/7/8/8 → 6/7/7/7, not the plan's one-per-tier cut. | Guaranteed worst-case clipmap coverage is `512·2^rings` m (the observer can sit at the edge of the outermost level's centre page). Only level 7 (131 km) sits wholly beyond the 45 km far plane; cutting lower tiers would end terrain *inside* it. Tier 0 stops at 32.8 km behind ≈89% haze opacity — pinned as a decision, not drift. |
| `1C-4` | Haze ambient carries the palette's `skyHorizon` colour at 0.9 strength. | Single scattering alone leaves the horizon several times too dim (real horizon brightness is mostly multiple scattering, measured ≈0.1 vs the sky's ≈0.7 radiance); tying the ambient to the sky's own horizon colour makes terrain fade into the sky it actually meets. `1C-5` then makes sky and haze the same integral, so the agreement stops being an input convention. |
| `1C-6` | Probe re-renders all six faces once per environment change (or >500 m altitude drift), not one face per frame. | The sun is static between clock scrubs; a six-draw 128 px pass over a ~100-ALU sky shader costs less than being six frames stale after a scrub. Diffuse SH is CPU-baked from the TS mirror (16 px cube, sub-millisecond) with a below-horizon ground-bounce attenuation (floor 0.25) so the bright horizon haze does not light undersides more than tops; validated against the πL identity. |
| `1C-8` | Shadow-through-haze is structural, not a tuned term. | Every consumer applies aerial perspective multiplicatively after shadowing (`color·T + inScatter`), so a shadow's contribution fades with the fragment's own transmittance and the haze in-scatter is correctly unshadowed. No `strength × transmittance` uniform exists to drift. |
| `2Z` | The evaluation surface (PRE_PHASE_4_REALIGNMENT §3) shipped before any Phase 2 pixel work. | Capture pinned at renderScale 1.0 (no letterbox), rAF-paced measurement, per-shot committed fps/hitch/max-frame ceilings, temporal-stability floors on a banked-turn motion scene, per-shot clocks (winter noon, night), the reference-viewport shot where the tier-1 cap binds, and the five §10.2 Phase-2 scenes. `gpuFrameMsP95` reads real values (R-4 was a sampling-window artefact; diagnostics now aggregate a rolling 600-frame ring the governor never consumes). Tier-1 MSAA 4×→2× (~34 MiB; alpha-tested foliage gets no MSAA benefit). One sanctioned rebaseline. |
| `2Z` | One allowlisted capture error signature. | Babylon 9.21.2's WebGPU backend gives each submesh its own material context but a shared material's textures bind only on the first submesh per frame; the remaining submeshes log `cloudShadowSampler … not found` exactly once when a fresh pipeline+context pair first draws, then heal. Verified pixel-free (all SSIMs hold). The receiver plugin now binds through `hardBindForSubMesh` with an always-bound 1×1 fallback and is enabled from construction (no mid-flight define churn); the residual burst is the allowlisted signature. Revisit at any Babylon bump. |
| `R-11` | Work ladder split into CPU-cost and GPU-cost levers. | GPU-bound windows never recover any lever; when Governor A is latched resolution-insensitive or floored (the reference-machine default state, R-6), the GPU ladder (reflection/cloud-shadow cadence, shadow-caster distance, vegetation distance) is the actuator. Pinned by two synthetic-trace tests. |
| `R-13` | Season is an ANCHORED deviation from the tuned midsummer world, and snow is appearance, not classification. | All seasonal kernels are exact zeros/ones at `TERRAIN_REFERENCE_DAY_OF_YEAR` (171), so the shipped look is bit-identical at the default clock (pinned). The snow blanket migrates in the baked colours; `classifyBiome`/`sampleTerrainTemperature` stay climatic — threading the offset into them would flip FOREST↔GRASSLAND with the calendar, which `2-18`'s own rule (species mix stays climatic) forbids, and would delete forests under winter SNOW. Seasonal humidity rides D-5's turbidity term; `EnvironmentDirector` fills `snowCoverage` from the same kernel; `surfaceWetness` stays 0 until a precipitation model owns it (recorded decision — nothing renders precipitation in Phases 2–5; the `2-1` weather bake carries the channel for that future consumer). Terrain pages re-bake per anchored ~15-day season bucket. |
| `2-0a` | The "sky-view LUT" is an (elevation × sun-relative azimuth) ambient table; the multiple-scattering LUT stays CPU-only. | The adopted march consumes ambient by (view elevation, sun-relative azimuth); a full Hillaire sky-view LUT would duplicate the sky dome's closed form for no additional consumer. The transmittance LUT upload is a verbatim rgba16float copy of the tested CPU bake (D-4's first GPU consumer), sampled with the exact `transmittanceLutUv` parameterisation (pinned by test). The MS LUT has no shader consumer, and uploading an unread texture is the dead-code habit this programme corrects. Blue noise is generated at startup (void-and-cluster, deterministic — the repo ships no binary assets). Scene depth is a `DepthRenderer` storing camera-space Z (clear 0 = sky); the cloud march reads last frame's map (the depth pass renders with the scene, after the dispatch) — one frame of clip latency, invisible at cloud distances. |
| `2-1` | Volumes baked on the GPU; the curl volume is not baked. | 128³ rgba8 perlin-worley base + 32³ rgba8 erosion detail on period-wrapped lattices (assertion 37 proves bit-exact tileability on-adapter; the check uses 1/256-grid coordinates because `u + 1` of an arbitrary f32 is itself unrepresentable). No shader consumes curl, so it arrives with its consumer. Precipitation is baked as a weather channel with NO renderer (R-16's open question, decided out loud: the channel completes the weather contract for a future wetness pass; nothing reads it in Phases 2–5). Cloud volume memory is a live `DYNAMIC_ALLOCATIONS` input (~9.1 MiB). |
| `2-3` | Anti-tiling via an ENDLESS weather field, not a toroidal clipmap. | The weather map is a camera-following 512² window (re-snapped on a size/8 grid, one cheap re-bake per ~12 km flown) onto a field of unwrapped world-cell hashes — it cannot repeat, ever, which is strictly stronger than the plan's toroidal clipmap of a repeating texture. The wind offset stays unwrapped (wrapping would teleport the pattern) and the window follows the advected position. Dual-scale base sampling (second fetch at an incommensurate 3.7× scale) breaks the base volume's own repeat period. The toroidal-addressing helper the plan wanted for `5-10` arrives with `5-10`. |
| `2-5` | Empty-space skipping + DISTANCE-ADAPTIVE strides shipped; the coverage prepass deferred. | The march long-strides (2× the coarse step) through zero density and steps back one stride on re-entry. All strides (fine, coarse, skip, start jitter) grow linearly with ray distance — ×2 at `stepDoublingDistanceMeters` (default 4 km, packed in `camera_up.w`) — and the sun-march step count decays on the same curve (floor 2); `cloudLighting` returns ambient-only when sun energy is negligible. Measured need, not speculation: the first 2A rebaseline put sky-heavy shots at 20–32 ms GPU p95 (slant-10km +24.9 ms vs pre-cloud) because horizon-grazing rays spent the whole 96-step budget at near-field stride inside the 5.7 km slab, 8 light fetches per sample — night paid the full light march for zero sun radiance. A per-thread density-sample counter lands in an atomic storage buffer, read back every 60 frames into `statistics.densitySamplesPerFrame` (assertion 39's number). The low-resolution coverage prepass stays deferred — skipping plus distance growth covers the measured cases. |
| `2-7` | Shadow footprint 24 km at 512² (47 m/texel). | The adopted shadow compute already WAS the sun-space tangent-plane footprint marching from the surface toward the sun — the `1/sunDirection.y` degeneracy does not exist in it. The receiver contract (`CloudShadowProjection` + inverse-sun reference-plane projection) is unchanged: with world-aligned tangent axes and a 24 km footprint the tangent-plane sagitta is ~45 mm, far below a texel. R-19 held — no transmittance multiply was added. |
| `2-8` | The cascade output is SLOPE; mips are real; distance filtering is Toksvig; the blend weights are Nyquist fades. | `deriveOceanSurface` stores RG = (∂y/∂x, ∂y/∂z) of the displaced surface (the cross-product ratio n.xz/n.y — n.y is the horizontal Jacobian, clamped at 0.05 where breaking waves pinch it), B = foam, A = Jacobian, plus a second-moment target (slope², rgba16float — rg16float is not a guaranteed WGSL storage format). Slopes of an additive height field average linearly, so box-filtered mips are correct by construction; `engine._generateMipmaps` rebuilds the chains after each derivation dispatch (recorded in the plan §13 with the encoder-ordering rationale, capability-asserted at startup). The fragment samples with `textureSampleGrad` and derivatives of the UNWRAPPED coordinate (fract's seam discontinuity would spike the LOD — GPU-gated with a negative control), sums fade-weighted slopes (heights add ⇒ slopes add; the 0.62/0.82/0.74/0.52/0.36 weighted average and its clamped normal-recovery are deleted), and folds the moment mips' MISSING energy into GGX roughness in α space (variance = E[s²] − (fade·E[s])², so a fully faded band contributes its whole mean-square slope rather than vanishing from the BRDF; α′² = α² + σ², σ² capped 0.25, perceptual roughness = ⁴√α′², still capped 0.34 for `2-9`'s IBL mip mapping — the adversarial review caught both the fade²·Var(s) extinction and a roughness-domain fold before the rebaseline). Cascade fades end where the band's longest wavelength spans two rendered pixels (CPU computes distances from the fovMode-AWARE pixel angle via the shared `viewScaleFromFov` — `camera.fov` is horizontal under 1B-11's pinned mode — and the shader fades on slant range √(r² + cameraHeight²), so altitude correctly fades bands that are sub-pixel at nadir); fading the vertex displacement too removes sub-vertex-spacing shimmer, and stored slopes clamp to ±8 so fp16 moments stay well-quantized (spacing 1/16 at 64), and the dead per-vertex `uv` lane is deleted rather than repurposed — on a camera-centred disk the fade is a pure function of ring radius (recorded deviation from the plan's per-vertex-attribute wording). Ocean WGSL hashes re-pinned (the 2-8a gate's deliberate-change flow). |
| `2-12` | Card trees with drawn trunks, distributed tint, baked occlusion, per-profile variants and character modifiers — the flight-test items, all five. | The cone/sphere prototypes are gone: crowns are 40–60 tilted alpha-tested quads and trunks are swept generalised cylinders (root flare, forks, per-species bark layers) from `prototypeGeometry`, unit-height with true proportions so the 2-11a radial aspect is the prototype's own crown/trunk radius. The atlas got its FIRST sampler: `DetailInstanceMaterialPlugin` gains a define-gated array path (per-vertex `atlasLayer`, the shipping 0.5 alpha test over Castano-preserved mips, baked 16-direction sky occlusion from vertex alpha darkening interiors ×0.42–1.0), bound via `hardBindForSubMesh` (the Z-1 per-submesh lesson) — `foliageAtlasMiB` moved 0 → 5.33 in the same commit. Trunks exist at MID too (no floating crowns), in their own back-face-culled bark batches (7 per-species bark materials — the radial aspect is a per-material uniform, and trunk aspects differ 30× from crowns). Tint is a perceptual DISTRIBUTION: hue σ 6.5°/8.5° (conifer/broadleaf), sat σ 0.10, value σ 0.12, means stand-correlated through the 2-11b field's tint centre, value anti-correlated with individual age — all three pinned statistically. Variants: per-species counts (5/3) capped by the new `treeVariantCap` profile datum (Low keeps 3); the variant byte's high three bits select character modifiers in the vertex stage — intact, lean, broken top, thinned crown (raised alpha test: the canopy loses texels, not quads), dead top (broken + bleached tint) at 55/15/10/12/8%. Under NullEngine the atlas is skipped (no raw 2D-array support) and everything else stays testable. |
| `2-12` close | The record IS the transform — instancing is forced OFF in the shader; the wrapper needs a WGSL varying remap under normal bias; R-21's price list is geometry, enforced per band. | Three on-adapter discoveries closed the item. (1) `forcedInstanceCount` routes PBR through the thin-instance path, which compiles `INSTANCES`/`THIN_INSTANCES` and rebuilds `finalWorld` from `world0..world3` instance attributes — absent by design in the 32-byte format, so Babylon's empty-buffer fallback zeroed every vertex: full vertex cost, zero pixels (R-20's spike used a `ShaderMaterial`, which has no INSTANCES semantics — the premise held, the composition with PBR was never tested). `DetailInstanceMaterialPlugin.prepareDefines` now forces both defines off; the mesh's world matrix rides the classic uniform path. (2) With CSM `normalBias` 0.035, `ShadowDepthWrapper` injects `shadowMapVertexNormalBias`, whose bare `vNormalW` never resolves after WGSL migration — wrappers on detail materials pass `remappedVariables: ["vNormalW", "vertexOutputs.vNormalW"]` (0-9's "none needed" was measured at `normalBias` 0, where the include compiles away). (3) Per-band prototypes: near ≤ 178 triangles (forked worst case trimmed from 220 — 45-quad cap + 4-side fork), mid = 12 quads at 1.8× size over a 4-side/3-ring trunk (40), far = three crossed crown-layer cards (6), banded by the LAW's radii in the appender — the interim all-bands-draw-near-geometry state integrated to 6.6 M triangles vs the law's 1.41 M and measured 29 ms GPU p95 vs 2-8's 13. Crown cards are 55% upright sails / 45% shelves (shelves alone vanish edge-on at horizon-level views); the needle atlas layers are boughs, not sprigs (10% → ~43% alpha coverage, 0.3 floor pinned for the five tree crown layers). `tests/gpu/foliage-material-compile.test.ts` reproduces the whole failure class in ~2 s: production stack, production CSM, zero uncaptured errors, depth readiness, and a rasterized-pixel floor. Two more findings closed the perf gap the trees exposed: shrubs now ride the SAME law falloff as trees with a hard cutoff at the mid boundary (the pre-law 60-near/6-mid share admitted 137k icosphere shrubs to 8 km — 11 M sub-pixel triangles), and geometry variants are capped per band (near = profile cap, mid = 3, far = 1 per species) because every (species, variant, band) mesh is a draw per chunk and the measured cost was ~26 µs of GPU per draw — Δgpu tracked Δdraws linearly across all thirteen shots (+171…+558 draws ≈ +2.6…+14.3 ms) while triangle-count changes measured ~0. Canopy renders in the alpha-test bucket (R-2E) and the cloud-shadow receiver predicate admits `ALPHATEST` (a surviving fragment is opaque; canopy must darken under cloud banks). Final capture: every 2B-close floor and ceiling held with card forests live — several shots faster than the coned 17 M-triangle system they replace (cruise-horizon 44.4 → 59.1 fps), the canopy's honest cost +1.4–2.4 ms on near-ground shots, ground-2m level at 29.7 fps with hitches 195 → 3. |
| `2-12b` | Card shrubs on the tree pipeline's exact conventions; the understory tint joins the perceptual distribution. | `buildShrubPrototype` (12–18 quads on 3–5 tilted stems, baked sky occlusion, hazelLeaf/juniperScale/sageLeaf layers) replaces the flat-shaded icospheres. Everything rides the 2-12 machinery: same atlas define, alpha-test bucket, double-sided lighting, per-material radial-aspect uniform from the built prototype, variant from the selection hash. Two variants per species in the near band, ONE at mid (a (species, variant) mesh is a draw per chunk — the 2-12 ~26 µs/draw law), with the shrub-law mid-boundary cutoff deciding existence. `shrubColor` was the last single-scalar brightness jitter: it now gets the tree treatment (hue σ 9.5°, stand tint-centre correlation, maturity darkening) and the shrub albedos move toward neutral because tint arrives per instance. Juniper/sage layers re-authored to card density (9.4/23.4% → ~34/38%). Bridge tests: shrub prototypes ≤ the mid 48-triangle allowance, shrub layers on the 0.3 coverage floor, understory hue σ > 4°, and the GPU compile test draws a juniper shrub on-adapter. |
| `2-13` + `2-13a` | Wind is three world-space bands off one per-frame field sample; season is R-13's kernel driving the tint's spare alpha lane. | Trunk sway / branch flex / leaf flutter displace AFTER the instance rotation (the absorbed band bent in local axes, so sway direction spun with yaw), sharing one `detailWind` vec4 (dirXZ, strength01, gust01) sampled per frame from `src/world`'s `sampleWind` at the observer on the pinned simulation clock — per-instance phase/response bytes carry all spatial variation, flutter is atlas-path-only and spatially decorrelated through card-local position. 2-13a: deciduous crowns turn per-species autumn hues then shed through the tint ALPHA lane (fragment lifts the alpha test 0.5 → 0.86 with leaf fall; conifers hold, dim 7%), crowns whiten toward the unorm8 tint ceiling below the descending snowline (full snow-white needs an albedo term — deferred to 2-17a), ±0.06 phenology jitter turns stands tree-by-tree. Latitude threads as a generation option; the worker derives it from its rebuilt world. The reference day is bit-invariant (winterFraction 0), so the tuned midsummer world and every capture baseline are untouched. |
| `2-14` | Crossfades are DISTANCE-baked complements, not timed animations — and the fade byte carries a direction bit because statistical complements double-draw. | Per-stem band memberships from the stem's own range at rebuild time (observer stashed), with 160 m dither margins at the near/mid and mid/far boundaries and a 420 m cull fade at the vegetation edge — a stem inside a margin lives in BOTH bands' immutable buffers with fade bytes that are exact complements: outgoing survives `bayer8 < fade`, incoming survives `bayer8 ≥ 1 − fade`, under one per-stem hash from the tint (identical across bands). At fade 0.5 a statistical complement lights the same pixels twice and leaves the rest empty — the direction bit is what makes the crossfade cover every pixel exactly once (pinned to ≤ 1 dither level of rounding slack across the whole fade). The Bayer threshold reads render-target pixels (governor-scale safe); the atlas path packs the byte into the atlas layer's f32 fraction so it costs no vertex output location. Generation residency stays two-tier — the three RENDER tiers are the law's bands. |
| `2-15` | Lithology is a shading model; debris follows the canopy; the snow shedding rule finds its live surface. | Displaced-icosphere rocks (320 tris) carry per-lithology NORMALS — limestone smooth-shared, granite/dark flat-duplicated — because the shading-model difference reads as rock type more strongly than colour; ~60% terrain-normal alignment rides the record's full orientation (`normalAlignedQuaternion`, tilt ⊗ yaw), the sink is the plan's `radius·flattening·(0.12+0.25·hash)`, and `applySnowCover` (extracted from the 2-13a crown path) whitens them with the slope-shedding weight that was VACUOUS for canopy — trees stop at slope 0.2, rocks reach 0.9. Clutter (log 64 / stump 40 / branchLitter 6 / mossCushion 24 tris) placs by canopy closure through the density field + a moisture bonus (soil-depth stand-in until 6-6 — DISCHARGED 2026-08-30, now a real `soilDepth` read through the shared `soilLitterFactor` law): wet closed forest ≈ 6× open grassland, moss gated on moisture. Near-field only, 85% normal alignment, 2-14 dither fades at each population's own edge (small rocks near, thinned ≥2.2 m boulders mid). Batches ride the shared 32-byte instance path — four clutter materials, three rock materials, zero new mechanisms. |
| `2-16` | The habitat grid says WHAT grows; the 1/d ramp says how much the frame affords; the observer joins the chunk signature. | Generation emits an 8×8 habitat grid per cell (archetype from the field's own terms — reeds wet+flat, fern shade+shelter, heather exposure, grass elsewhere — plus coverage and seasonal tint) and the appender expands it into ≤ 48-triangle blade patches: hash-jittered 2 m candidates (world-keyed, so positions never slide), acceptance `min(1, 0.2R/d)`·coverage, 30 m edge dither. The closed-form integral at the tier-2 220 m radius is ~0.66 M triangles against the ≤ 0.9 M Balanced exit budget, pinned as arithmetic. `grassRadiusMeters` is the §5.3 first tier knob (90/150/220/320). One parameterised blade builder serves all four archetypes (different layer, blade count, arc, width). The fix that fell out: per-chunk signatures now carry the 64 m-quantized observer — 2-14's distance-baked fades and this ramp are functions of the observer, and chunks with unchanged residents kept stale fades until residency churned. |
| `2-17` + `2-17a` | The impostor atlas is a CPU bake — a pure function of the seed — and the bake corrected 2-13a's shed mechanism. | `ImpostorAtlas.ts` rasterizes each species' near prototype from 16 hemi-octahedral views (the exact card composition: foliage texels × material albedo × occlusion floor) into 4×4 grids of 64² tiles × two season buckets, mipped coverage-preserving, uploaded as two `RawTexture2DArray`s (albedo, normal+depth) — 9.33 MiB measured, the recorded §5.2 arbitration against the plan's non-closing 128² sketch. The far band renders ONE cylindrical billboard per stem: the containing hemi-octa grid triangle's three views blend barycentrically (view snap under bank averages away), the buckets cross-fade on the card dissolve's winterFraction window, and the FREE far-band variant byte (geometry variants collapse) carries a per-stem hash → view-phase offset + mirror, so no two neighbours share aspect and phase. Impostors neither cast nor receive shadows — that frees the cascade varyings the blend lanes consume. Byproduct correction: 2-13a's threshold-lift shed could not drop painted leaves (interiors are alpha ≈ 1; 17.1% → 16.3% measured) — the shed is now a uv-cell dissolve shared verbatim between the card fragment and the bake (oak bare bucket 30.4% → 17.6%, conifers byte-identical). Exit criteria are Node tests because the bake is pure: the mean-colour envelope, the phase/mirror distribution, determinism. Normal+depth shading hookup deferred (recorded). |
| Gate 7A (`7-3`) | The star catalogue is AUTHORED for the bright end and GENERATED below it — no third-party data file enters the build. | `RENDERING_PLAN.md` asks for the Yale BSC (~9,100 stars). What it asks the catalogue FOR is stated in the same row — "makes constellations correct" — and that is a property of the bright end. So ~190 stars to magnitude ~3.6 carry their real J2000 positions, magnitudes and colour indices, and the naked-eye background below that is generated to the observed magnitude-count law (fitted through the V ≤ 3 and V ≤ 6 anchors, within 2% over the range it is used) with a galactic-latitude concentration. Every asset in this renderer is synthesised from the seed; vendoring a third-party table with its own provenance question would be the one exception, for a property the bright end already delivers. The transcription is guarded by GEOMETRY, not by review: Orion's belt collinear and 2.7° long, Dubhe→Merak extended finding Polaris within 8°, the Summer Triangle's three sides, the Southern Cross's, and the ten brightest in order. |
| Gate 7A (`7-1`) | Night's ABSOLUTE scale is art-directed by two named constants; every RELATIVE quantity is physical. | A full moon delivers 0.25 lux against the sun's 120,000 — 4.8 × 10⁵ — and the beauty target is fp16, whose smallest normal value is 6.1 × 10⁻⁵. At the sun's own calibration moonlit ground lands at 1.1 × 10⁻⁵ and quantises to nothing before any post-process sees it. Rendering night photometrically needs a scene PRE-EXPOSURE applied to every light and every shader that writes radiance, which `1C-2` deliberately did not build (assertion 29 forbids a shader multiplying its own exposure; a pre-exposure is exactly that, everywhere). `MOON_PEAK_LIGHT_INTENSITY` (0.055) and `STAR_ZERO_MAGNITUDE_SCENE_VALUE` (0.5) are therefore chosen; phase, the opposition surge, altitude, the perigee/apogee distance term, magnitude ratios, extinction and spectral chromaticity are all computed. `7-2` reads the TRUE lux for its rod/cone decision, so perception is physically driven even though the buffer is not. `7-4`'s clustered lighting meets the same 10⁵ range with light points and is where the pre-exposure decision belongs. |
| Gate 7A (`7-2`) | Both constants the realignment named are reopened, and both are derived now. | `exposureForState`'s hard clamp of 2.6 becomes `MAX_EXPOSURE` — the curve's own value at the illuminance where human vision hands over to the rods (0.03 cd/m² through a Lambertian 0.2 albedo), which evaluates to 4.698 and moves only if the curve or the scotopic threshold moves. `ambientIntensity`'s unconditional 0.05 now follows the sky's own illuminance, exactly 0.05 at the reference day+clear key and floored at a fifth of it (`NIGHT_AMBIENT_FLOOR_SCALE`) for the same fp16 reason above. `1C-10`'s 40-lux diffuse-skylight floor — a lit car park, and the actual cause of "at 22:00 the ground is black", because no moon could show above it — is replaced by a twilight tail running to a 0.0015 lux airglow floor. |
| Gate 7A (`7-2`) | The rod response is half-saturated at the SCENE's key luminance, not the physical one. | Measured: normalising Naka–Rushton against the real 10⁻⁴ cd/m² of a moonless field drives every pixel to full saturation and returns a flat grey image — the first night capture of this item. Adaptation puts the mid-tone where the mid-tone is, whatever the absolute level; the perceptual difference (desaturation, the Purkinje blue, the acuity loss) rides `rodFraction`, which is computed from the physical adapted luminance. The scotopic pass is FIRST in the post-process chain so it sees scene-referred linear radiance, which moves `1B-11`'s MSAA ownership one stage earlier with it. |
| Gate 7A + perf-debt (capture) | One sanctioned rebaseline; the `night` shot gets its own SSIM threshold; the fps floors are NOT re-pinned. | Both bodies of work change pixels deliberately — night entirely, vegetation everywhere — so the committed baselines were rewritten once, at the close, and `VITE_PERF_SHOTS` was added so a partial run can never be the thing that rewrites them. `night` relaxes to 0.96 (the resize-path shot's precedent) because Gate 7A made it the noisiest shot in the set for a structural reason: the rod response applies a large gain to a very dark image, and the cloud pass's temporal jitter — invisible by day — is amplified with everything else. Measured 0.972 between two runs of an identical build. The `minFps`/`hitchCount` ceilings are left exactly as the reference M-series machine measured them: this pass's captures ran on a different box, where HEAD ALREADY failed `slant-10km` (45.5 against a floor of 49) and `high-10000ft-down` (41.1 against 47) before any change, and where the same unchanged shot measured 12.89 ms GPU on one run and 5.39 ms on another. Substituting those numbers for the reference machine's would corrupt a committed gate. The counters that ARE portable — `drawCalls`, the new `vegetationBatches`, `triangles` — are what this pass reports. |
| Perf-debt pass | Vegetation is a DRAW-CALL workload, and no lever §5.3's trade-off rule permits can close its frame row. | The `2-17`-close ledger named three rungs. Measured, they are worth: instance-buffer reuse — a CPU and allocation win, no draws; near-field density and radius tuning — almost nothing, because draws scale with (chunks × meshes) and a presentation chunk is 4,096 m, wider than the whole near+mid field at every tier; shadow-pass simplification — the trunk casters cannot go, because at low sun their shadow is the gap between the tree base and the crown's. What moves the number is mesh COUNT per chunk, and §5.3 puts crown variants per species on the "not a budget knob at any tier" list. The far band's seven per-species impostor meshes became one (the quad never differed; only the bake frame did, and that is a per-instance uniform row now), the §5.3 band radii were adopted, and the residual is priced: merging crown and trunk into one mesh takes tier 1 from 347 to 186 draws at identical fidelity, and carries a real risk R-2E already names — trunks would leave the opaque bucket that fills depth before the canopy shades. `VEGETATION_DRAW_CEILING` and `VEGETATION_FRAME_DEBT_RATIO` pin both the state and the gap. |
| Perf-debt pass | Instance allocations are RECYCLED, never destroyed in flight. | Babylon 9.21.2 never calls `Buffer._increaseReferences`, so five typed views over one interleaved allocation leave its reference count at ONE and a single `dispose()` destroys the GPU buffer. Destroying one a submitted command buffer still references is a validation error and WebGPU rejects the whole submit — the symptom is a black frame at a suspiciously high frame rate. A four-update grace window produced it immediately; a six-hundred-update one (ten seconds) still produced it, with a diagnostic confirming no live mesh held the buffer at eviction. A pool is not a guess: overwriting a pooled buffer is queue-ordered after the previous submit and can never be an error, and memory is bounded by the peak working set, which `detailInstanceBudget` already books. (The pre-pass code leaked these allocations outright, which is why the class had never been seen.) |
| Perf-debt pass | Rendered-share thinning selects the CANOPY, not a random sample of the forest. | Gate 2C's ≥0.55 crown-cover criterion was the one unautomated box, and automating it failed at 0.26. Cause: the ecological field authors ~400 stems/ha across every age class (measured mean crown radius 3.40 m, median 3.15 m, p90 1.78 m — mostly saplings, as a real stand is), and thinning to the law's ~70/ha by a UNIFORM key keeps saplings and dominants in equal proportion. Ranking by crown radius draws the widest crowns (measured mean radius 5.80 m, cover 0.53–0.56) — which is the "60–80 stems/ha with 6–7 m crowns" the law was priced against, and what a canopy IS. The key keeps every property `D-2` requires: deterministic, uniform on [0, 1) by construction, and nesting. `selection` is untouched and stays the appearance hash, so nothing about how a drawn tree looks moves with it. Tier 1's near cap went 70 → 78 stems/ha to clear 0.55 with margin; total rendered stems still fall 19,445 → 15,441 because the band radii moved further. |
| `2-17` close | Crossfades moved into the fragment; the hitch metric moved out of the quantization band; the near-field cost is recorded debt. | 2-14's baked fade bytes coupled fades to chunk rebuilds, and at approach speeds the capture measured the rebuild train directly. Final form: tree-band fades compute IN THE FRAGMENT from the stem's true camera range — near owns `[0, fNear)`, mid `[fNear, fMid)`, far `[fMid, fCull)` of the dither square, so complementarity is structural and continuous; the record's fade lane carries a band CODE for tree materials; memberships (slack 96 m) rebuild on a 64 m quantum for FRONTIER chunks only, one per update, and empty-window duplicates collapse in the vertex stage. Single-edge populations (rocks/shrubs/clutter/grass) keep baked fades. Z-2 amendment: hitch threshold 3× → 4× — the third re-pin of the same failure mode (a threshold inside the workload's vsync-quantization band), 190–236 phantom hitches collapsing to 3–12 real ones. The floor re-pin is MIXED and honest: far field rises (impostors), near-field airport shots drop to ~21 fps carrying the complete understory — OPEN PERF DEBT against §5.4 touching the G-target tier, with the remaining rungs named (R-21 near-field constants, buffer reuse, shadow-pass alpha simplification) and scheduled before the Phase-4 G-target gate. Hard-won contracts: `getBatch` INHERITS `receiveShadows` from the prototype (it silently forced true, and the impostor fragment overflowed the 16-input limit — a black frame from one invalid pipeline in a render bundle); grass candidate heights bilinear from the habitat grid, never per-candidate terrain samples; and every define a plugin can set MUST be DECLARED in its constructor define list — Babylon silently drops undeclared keys written in `prepareDefines`, and the undeclared `DETAIL_BAND_FADES` compiled every tree onto the legacy fade path where band codes read as ~zero fades (an invisible forest the GPU rig's whole-frame pixel floor could not see; the rig now asserts visibility PER SCREEN REGION). |
| `2-11b` | Stand identity is a field, not a lattice; the appearance domain has its own spectrum gate. | The 32 m `{standAge, dominantChoice}` block lattice is deleted: stand age, dominant-species mix and the `2-12` tint centre come from two octaves of smooth value noise (137 m + 61 m, second octave rotated 0.37 rad off-axis) evaluated at the STEM'S OWN world position — invisible while every tree was a cone, glaring once species get silhouettes. The appearance-spectrum test sweeps species/age/height/tint-hue channels below the stand band with attribute-weighted projected power, checks stand correlation directly (near pairs share age; far pairs do not), and carries an ANOVA negative control — within-block variance over total — because a per-block-constant channel's 32 m spectral line integrates to ZERO over uniformly filled blocks (measured: the synthesised old lattice reads ~0 there but fails the variance ratio at < 1e-6, while the live field measures > 0.05). `DetailTreePlacement` gains `standAge` for `2-12`/`2-13a`. The positional scatter test's >120 m phase bound widened 0.10 → 0.12: the stand field couples weakly into survivor positions through crown-size spacing — intended structure, not a lattice (lattice failures measure 0.78/1.17+). |
| `2-11a` | Matrices left the vegetation path: 32-byte records, vertex-stage transforms, generator AABBs, wrapper-guarded shadows. | The §3.5 layout at its widths, regrouped into WebGPU-legal quads (a lone f16 is not a vertex format; the spare byte upgraded `radialScale` to unorm16 — full ledger in the plan's §13 `2-11a` row). `radialScale` is a slenderness multiplier over [0.5, 1.6] against a per-material aspect uniform carrying each prototype's authored radius-per-height, so one record serves trunk and crown batches that decode it differently. Batches build pooled interleaved buffers during generation and upload one immutable 32-byte-stride `Buffer` as five typed instanced `VertexBuffer`s; `forcedInstanceCount` draws them (R-20's proven mechanism). With no matrix buffer, the 0-9 `ShadowDepthWrapper` incantation is load-bearing, not optional: an undisplaced depth pass would collapse every shadow instance onto the batch origin. `DetailWindMaterialPlugin` is absorbed (its GLSL branch never executed and dies with it); snorm16 quaternion error 0.0018°; 400k instances 38.4 → 12.8 MB. |
| `2-11` | All-card-layer atlas with CPU coverage-preserving array mips; the GPU upload waits for its first sampler. | Babylon mips only layer 0 of a `Texture2DArray` (verified: `webgpuTextureManager.js:716` hardcodes `faceIndex = 0`), and Castano coverage preservation is computed, not blitted — so `TextureArrayMips.ts` is a CPU reducer that walks every layer, applies a caller-supplied kernel per level and uploads via `updateMipLevel` (`3-1` reuses it with a Toksvig kernel). The atlas synthesises all 16 card layers deterministically (leaves, needles, barks, grass blade, fern, heather, reed, hazel, juniper, sage, litter — everything drawn as a card, per the amended layer list), with alpha dilation before mipping and assertion 45 pinning every layer's mip coverage within 3 points of mip 0 at the shipping alpha test. Measured 5.33 MiB. `createFoliageAtlas` is NOT called yet: `2-12`'s card trees are the first sampler, and uploading an unread texture is the dead-code habit this programme corrects — the `foliageAtlasMiB` budget row moves in the same commit that creates it. |
| `R-21`/`R-20` | The rendered-density law replaced D-2's constants, and the 2-11a instancing premise is proven on-adapter. | R-21's demanded re-derivation bit exactly as predicted: D-2's Phase-1 constants (70/ha to 1,400 m, `(1000/d)²` floored 0.04) integrate over tier 1's saturated closed-forest disc with Phase 2's real prototypes to ~94,000 stems ≈ 17 M triangles — 10–19× every vegetation frame row. The mechanism survives; the constants became the three-banded per-tier law in `renderedDensity.ts` (near = full geometry at crown-closure stems/ha, mid = cards at inverse-square falloff, far = impostors at a floor share), pinned live against per-tier woody triangle budgets with D-2's 17 M integral as the negative control, exposed as a profile datum (`renderedDensityLaw` — the tier rule), and consumed by the runtime thinning today (mid/far stems draw with current geometry until `2-14`/`2-17` land their bands). Closure comes from the per-cell cap being the closure density itself — closed cells keep their interiors, open cells render everything they authored. R-20: `tests/gpu/instance-format-spike.test.ts` proves `forcedInstanceCount` + manual instanced `VertexBuffer`s with snorm16x4 orientation and unorm8x4 tint on the adapter (rotation observable as geometry, tint as colour); the named fallback if a future adapter fails it is the 64-byte float32 layout, mechanism unchanged. |
| `2-10` | The planar-reflection capture is retired; the receiver contract and lake hysteresis survive verbatim. | The `MirrorTexture` capture — four private Babylon scene fields, a per-tier budget ladder, a governor cadence rung and one extra camera — is deleted; with the 1C-6 probe live on both water materials and water roughness capped at 0.34, the sky cube covers what the mirror pass existed for. Kept Class-T (preserved, not improved): the receiver WGSL/uniform contract (both water materials stay compiled against `samplePlanarSceneReflection`, bound to a shared 1×1 zero-confidence fallback texel so `5-12` re-points a lake capture without touching a shader), `selectPlanarReflectionPlane` with its enter/release hysteresis, and `acceptsInlandPlanarReflection` with the per-mesh gate in `HydrologySystem`. Reconciled in the same commit per the plan: the governor's planar-reflection-cadence rungs are gone (a lever must never be attached to nothing), the boundary test's grandfathered `.tier` list shrank, and `OTHER_DETAIL_ALLOWANCE_MIB` gives back the mirror's MiB per tier. |
| `2-9` | One physical sun lobe; lit advected foam; crest SSS; probe-fed reflections; R-24 culls, not clamps. | Both water surfaces light their sun through the shared Karis representative-point lobe (α widened by the sun's 0.004675 rad angular radius over 2, energy renormalised by (α/α′)²) — `pow(·,3200)·16`, `pow(·,1800)·11`, `×2.6` and the gain-of-four multiply are deleted, and the fake specular sun discs died with them (the analytic `reflectedSky` is now fallback-only sky colour). Environment reflections sample the 1C-6 probe cube (raw ShaderMaterials never see `scene.environmentTexture` — the renderer binds the probe RTT explicitly; a 1×1 fallback cube keeps WebGPU materials ready before the probe exists, nullable under NullEngine), with roughness→mip calibrated 0.075→0 / 0.34→2 against the probe's box chain. Foam is lit Lambertian (sun × nDotL × visibility + sky ambient) under a two-octave Worley break-up advected by wind (ocean) or flow (rivers); albedos stay call-site values. Crest SSS transmits sun through the summed displacement height the vertex previously discarded — intensity is one of Gate 2B's two declared knobs. R-24 implemented as the realignment words it: a maximum terrain grade (0.09) above which NO ribbon is emitted — rivers keep only the run downstream of their last too-steep segment or vanish (behavior-pinned in the hydrology suite), and `maximumRivers` drops 10 → 7. `sunAngularRadiusRadians` joins `AtmosphereSnapshot`. The adversarial review caught two shading defects pre-rebaseline: the crest-SSS grazing factor was sign-mirrored (inert aloft, attenuating exactly the backlit-crest hero shot), and the probe cube carried the sky dome's 40×-radiance painted sun disc — sub-texel at 128 px — double-counting the sun against the Karis lobe as a blocky mirror blob. The disc is now suppressed during probe captures (`sunDiscVisibility`, flipped around the cube's bind/unbind), which also stops PBR IBL double-counting direct sun: the probe cube is the AMBIENT environment by contract. |
| `2-8a` | Water shading extracted; the byte gate is a WGSL text hash, not a capture diff. | `WaterShaders.ts` owns fresnel, both GGX assemblies (the ocean's combined lobe and the hydrology's split pair stay separate exports — different BRDF assemblies, unified by `2-9`, not by the extraction) and a `reflectedSky` generator whose genuinely divergent constants (horizon falloff 2.5/2.3, overcast palettes, sun disc 3200×16 / 1800×11) are named `WaterReflectedSkyParameters` at the two call sites. Assertion 41 is implemented as a pinned SHA-256 of the composed ocean WGSL — the shared blocks recompose the pre-extraction text character for character, so the rendered output is identical by construction; two captures of a temporally-jittered volumetric sky are never byte-equal, a text-identical shader is (recorded deviation from the plan's capture-diff). Hydrology's text canonicalises (parameter renames only — `cosine`→`cosTheta`, `horizonAmount`→`horizon`, `solarGlare`→`sun`), which is output-neutral. Deliberate shading changes re-pin the hash in the same commit — drift stays impossible, change stays explicit. |
| `2-0` | `CloudShaders.ts` adopted wholesale; raymarch converted to compute; ray basis everywhere; R-19 honoured. | All three modules run through Babylon `ComputeShader` (the ocean's pattern) — the raymarch writes two rgba16float storage textures instead of an MRT fragment pass, so no MRT plumbing needs to exist. The adopted motion-vector path (`previous_view_projection`) was deliberately NOT adopted: it is the 1A-4 stale-matrix bug class, and the temporal resolve reprojects from the previous ray basis + absolute camera delta instead (CloudReprojection's invariant, ported into the compute). No `shadow × transmittance` term was added anywhere (R-19/D-7 — haze-through-shadow stays structural). The shadow map stores rgba16float (r32float is not filterable and every receiver filters). The config is truthful: profile/schedule tiers enter through `resolveVolumetricCloudConfig` overrides and assertion 35 proves every field reaches a uniform block. Assertion 36 compiles and dispatches all three modules on a real adapter. |
| `4-0` | The terrain spine contract lands before its eleven consumers; the season bucket rides the SLOT key, not the page key. | Exactly one channel family is season-dependent — land cover — so page identity is untouched and the season costs eviction bookkeeping instead of a re-architecture. Two buckets stay resident and cross-faded AT ALL TIMES: snapping when the clock is static saves no memory (the atlas is sized for two either way) and quantises the snowline to 15-day steps exactly when the user is looking at it. `seasonBucketBlend` is computed on bucket CENTRES with modular arithmetic — a linear `lo + 1` asks for bucket 24 in late December — and a test walks all 365 days. Two defects surfaced here and neither was in the plan: `validateWorldPageLayout` required a `2^n + 1` height core, so the shipped `WORLD_PAGE_LAYOUT` had been failing its own validator since Phase 0 (nothing called it); and `inspectWebGpuCapabilities` enumerated `adapter.limits` with `Object.entries`, which returns NOTHING because `GPUSupportedLimits` keeps its limits as prototype getters — the limit map had been empty since Phase 0 and `4-0`'s startup assertion would have been vacuous. |
| `4-1` | Split-origin lattice addressing removes the coordinate-magnitude term entirely, and the supported world radius is set by the lattice wrap rather than by precision. | Measured on the reference adapter over 40,960 / 12,960 / 3,840 points × five filter widths: **3.78 mm at ±10⁴ m, 3.44 mm at ±10⁵ m, 2.37 mm at ±2.8×10⁶ m** — flat. Naive f32 over the same probe: 4.5 mm / 60 mm / 3.47 m. `RENDERING_PLAN.md:347`'s `< 0.05 m at 5×10⁶ m` was off by ~70× against naive f32 and is now met by a factor of 20 against a bound the plan did not think reachable. What remains is f32 accumulation over ~50 terms, so criterion 4 is **5 mm, not 1 mm** — and 1 mm was never the binding number: the runway crown carries 5.8 mm of chord error at L0's own 8 m vertex spacing. Three page constants are hoisted for the same reason the origin is (band-limit weights, the three variance-kept scalars, and every lattice's pre-mixed seed), which removes 34 integer hash chains per texel and cannot change the answer. |
| `4-3` | `maxDeviationFromParent` is the page's largest SECOND DIFFERENCE, measured in the generation dispatch, not a re-evaluation at the parent's filter width. | It is exactly the vertical error a parent's half-rate sampling makes, which is the numerator CDLOD's screen-space error needs, and it costs a one-texel workgroup apron instead of 34 more `valueNoise2D` calls per texel. Reduced through orderable-u32 atomics in the same dispatch — a workgroup reduction plus three atomics IS the parallel reduction, one pass earlier. Measured 0.135 m on an L0 page and 85.2 m at L4. Found while wiring it: `new StorageBuffer(engine, size, STORAGE\|READ)` drops WRITE, so `update()` silently does nothing and the atomics reduce against a zeroed buffer whose min slot decodes to NaN. |
| `4-7` | One bake, one owner, one format — and a kernel-page binding that no live code READS disappears from the module's reflection. | Sky visibility is the GTAO `cos²(horizon)` form (the cosine-weighted integral of the unoccluded slice, not a tuned falloff); the bent normal is the open-arc average; eight azimuths are stored as the max of each marched pair, because a horizon that over-shadows is a shadow slightly too long and one that under-shadows is light leaking through a ridge. The march's geometric growth ratio is PER JOB: a baked-in ratio overshoots by four orders of magnitude at coarse levels and every step past the pyramid's edge is wasted on a clamped sample. Measured: visibility varies 238–255 over a 270 m relief page, and two independently baked adjacent pages agree to 14/255 across the shared edge — which is the global height pyramid doing its job. Tint prunes unreachable functions, so a shader that includes the kernel only for its math helpers leaves Babylon's bind group carrying an entry the layout does not have, which invalidates the whole command buffer and writes zeros; `tests/gpu/terrain-compute-compile.test.ts` exists so a WGSL error is a one-second failure rather than an eight-minute `dispatchWhenReady` timeout. |
| `4-4`/`4-5` | One mesh draws the ground; the geomorph closes cracks analytically; and Babylon 9 tree-shakes the thin-instance API. | 151–172 CPU tile meshes with skirt walls and hole-punched index buffers become one 33×33 unit grid thin-instanced over nodes selected by MEASURED screen-space error — terrain draw calls are `1 + shadowCascades`. The quadtree roots at level 9, not 7: the root ring is the FLOOR COST, and at level 7 the 45 km far plane needs ~121 roots, three quarters of Low's entire 160-node budget spent on ground at the horizon. `subIndex` carries page PARITY so the vertex shader can address the parent page's texels, and at `morphK = 1` that load is an exact texel — which is what lets skirts go and `backFaceCulling` become true. Three things bit: `thinInstanceSetBuffer` and `thinInstanceCount` are `undefined` without `import "@babylonjs/core/Meshes/thinInstanceMesh"` (the mesh silently draws nothing); the surface plugin was enabled only by `setArrays`, so the displacement path would have been off for the ~10 frames `3-1` takes and the aircraft would spawn inside a flat plane (a 1×1 placeholder array stands in now); and `getCustomRenderList` returns meshes, so the per-cascade subset is expressible only because `4-5` stands up one caster mesh per cascade. |
| `4-6`/`4-6b` | `classifyBiome`'s threshold cascade is deleted; the biome id is a READING of the classifier's weight vector, taken at the reference day. | Ten smooth suitabilities, softmaxed with a driver-jittered temperature so ecotone SHARPNESS varies, top-4 renormalised. The reference-day reading is load-bearing: `dayOfYear` drives the snow weight, snow is PAINT, and letting it move the dominant material flips forest to snow with the calendar and deletes every forest each winter — which `2-18` forbids in as many words. Caught by `world.season.test.ts` the moment the seasonal shift reached `classifyBiome`. `R-27` settles here: `chooseTreeSpecies`, `chooseShrubSpecies` and the wildlife habitat predicate all read `landCoverHabitat`. The climate chain moved into the kernel's lattice table and is band-limited for the first time — `filterWidthMeters` had been validated and never used since Phase 0. One import cycle surfaced: `surfaceMaterials.ts` imported the `@/src/world` barrel, and routing `classifyBiome` through the classifier closed the loop and left `SurfaceMaterial` undefined at module-init, failing 30 test files at once; it imports `world/types.ts`, a leaf, now. |
| `4-9` | The runway earthworks are a transliteration that CONSUMES `3-9`'s airport SDF, and `sin`/`cos` are hoisted as f64-computed constants. | WGSL specifies `sin` and `cos` to an ABSOLUTE error bound rather than a relative one, so recomputing the heading rotation per texel is the single largest divergence source in the item; hoisting removes it. Measured **0.298 mm** over 23,805 probes spanning the platform (5,023), the batter (6,933) and untouched ground (11,849) — all three regimes, because the two physics entry points take different short-circuits. The page stores the AIRPORT-FLATTENED height, so the CDLOD AABB is correct over the airport and the runway exists at all now that the CPU tile path is gone. `generateTerrainTile` and `src/world/tile.ts` are deleted in the same commit as invariant test 3's old form, which would otherwise have kept passing against a render path nothing renders. |
| Phase 4 close | **Every automated test was green and the screen was black.** Four defects that only running the app could find. | (1) The terrain's `ShadowDepthWrapper` needed `remappedVariables: ["vNormalW", "vertexOutputs.vNormalW"]` — `2-12`'s amendment, which `0-9`'s "none needed" predates because that spike ran at `normalBias = 0`. At the atmosphere's 0.035 the injected `shadowMapVertexNormalBias` references the varying by its bare GLSL name, the shadow VERTEX module fails to compile, and the frame's whole command buffer is invalidated — sky included. The wrapper test now runs at a nonzero bias. (2) A TypeScript ternary in WGSL, which has none: no GPU test compiled the surface plugin with `TERRAIN_SURFACE_PAGE_CHANNELS` defined. One does now, and it fails on any uncaptured GPU error rather than only on its own assertions. (3) Fresh thin-instance arrays every frame destroyed buffers a recorded render bundle still referenced (`used in submit while destroyed`) — the instance buffers are allocated once at the node budget and updated in place. (4) The CDLOD page-local varying carried a NORMALISED position while the fragment divides by the page extent, so every fragment sampled its page's first texel: a per-page constant material, which renders flawlessly as a uniform desert. **The pattern in all four is the same** — each is invisible to a test that compiles a shader in isolation or asserts on numbers, and obvious in one frame. `perf:capture` is the instrument that closes the gap, and its mean-luminance floor caught three of the four the moment it was pointed at the new path. |
| `4.5-0` | `resetDrawCache` on a rendered mesh orphans its `ShadowDepthWrapper` registration; every wrapper is now built by the guarded factory, and the boundary test forbids constructing the raw class. | The intermittent "Unable to continue flight" load crash (`createBindGroup: Required member is undefined`), root-caused live: the wrapper records each submesh's forward effect via `onEffectCreatedObservable`; `resetDrawCache` destroys the forward draw wrapper without telling it; on the submesh's FIRST depth render `_makeEffect` copies the destroyed wrapper's `defines` (undefined → null) into permanently cached depth params; PBR's `bindForSubMesh` silently early-returns on null defines and the depth draw executes against an empty material context — the exact signature `TerrainClipmapSystem`'s "bisected to the STALL" incident recorded (the stall widened the window; the mechanism was never removed). Trigger in flight: `bindInstanceBuffers` resets a growing foliage batch in the same frame graph that renders the CSM pass first; loads crash whenever a batch's first cascade appearance lands on a growth-rebind frame, which during streaming is most loads. The guard (`core/guardedShadowDepthWrapper.ts`) refuses to build depth params from a destroyed forward wrapper — a one-frame not-ready skip; the next main-pass render heals it. Same commit: the terrain CASTER meshes no longer `resetDrawCache` after the `3-1` material-array build — layerMask 0 means their forward wrappers are never recreated, so the reset gave them the same two death branches (crash, or silently never casting again) while refreshing nothing a depth pass reads. `tests/gpu/shadow-depth-wrapper-reset-guard.test.ts` pins both halves with a non-vacuity control: the raw wrapper must still die in the reset-then-cast window, or the guard is dead armour to retire. |
| `4.5-A1` | CDLOD selection is a GLOBAL max-screen-space-error priority queue with a mandatory 2:1 neighbour clamp. **D17's "breadth-first by level, nearest-first inside a level" bullet is amended; its level-9 roots, `subIndex` parity lane and budget-remainder counting stand.** | The per-level split loop CONVERGED: with tier 1's 240-node budget and real kernel deviations it terminated with the whole world at L5-L7 at BOTH 150 m and 3,000 m — kilometre-scale height texels under the aircraft, which is what the reported "splotches of solid colour" were. The unconstrained criterion wants >= 2,300 nodes, so raising the budget could not fix it. D17's rationale for breadth-first (depth-first starves a quadrant, "the ground behind the aircraft disappears") is satisfied a different way: the root ring is emitted first and a split only ever SUBDIVIDES, so horizon coverage is complete by construction. The clamp is not complementary — the analytic crack closure guarantees seam identity across ONE level, and with skirts deleted a two-level step is a hole; a split is therefore applied only with its whole forced-split closure, so the invariant holds by construction rather than by a repair pass (assertions 107, 108). Two measured consequences: the NODE BUDGET binds before `cdlodPixelThreshold` at every shipped tier (identical node sets at thresholds 3 and 6), so the budget is the operative knob and was re-tuned 160/240/320/448 → 224/320/448/640; and the L10 parent pages an L9 node can never morph into are no longer streamed. |
| `4.5-A2` | The seven channel-family atlas textures are BILINEAR; only the PRIMARY id lane may be read from a filtered fetch, and the season-bucket ids come from the low bucket alone. | `3-0` ordered the material axis so that a filtered id lands between two materials that actually meet — and the atlas was created NEAREST, so that ordering bought nothing and every resident page rendered as hard-edged single-material blocks at the splat's 2·2^L m grid. Lanes y..w are the classifier's independent 2nd..4th picks per texel, not axis neighbours: filtering them sweeps through unrelated integers, so the page path supplies no secondary id at all (-1, never 0 — 0 is SAND) and minority cover needs a per-texel fetch, which is its own item. The latent `mix(idLo, idHi, blend)` is fixed to take `idLo` per its own comment; it was masked only because the two buckets differ in how much snow they carry rather than in which material they name. A 1-texel bilinear footprint cannot cross a slot: the fragment clamps into `[gutter, gutter + core]` and the bake writes the full 4-texel gutter (assertion 109). |
| `4.5-A3` | The provisional ecotone axis is walked PER VERTEX in the CDLOD vertex path from the just-displaced height; the CPU lane carries only the "no height texels" grass guard. Channel slots missing from a height-resident page are re-requested, and a season-bucket rollover re-bakes the splat. | The fallback was one packed constant per NODE, so wherever a page held no channel slot the ground was a single material across up to `512·2^L` m — the other half of the "splotches", and worst exactly where streaming is worst. The walk moved wholly into the shader (`TERRAIN_PROVISIONAL_AXIS` supplies the constants) so there is one derivation site; the CPU keeps only the decision the shader cannot make, which is whether the height it sampled means anything. Two adjacent holes closed with it: `updateAtlasResidency` used to `continue` past a height-resident page after a `touch()` that no-ops on a missing channel key, so one refused channel admission left that page on the fallback until the HEIGHT page was evicted; and the splat bake keys its slot on `invariantSlotKey` and bakes once, so the snowline froze at whatever day the page streamed in on (assertions 110, 111). |
| `4.5-B1` | A height page's TEXELS are published at dispatch-submit and its bounds/deviation arrive a readback later — gated on a named `PAGES_ARE_FINAL_AT_DISPATCH` so `5-4` deletes the fast path rather than fighting it. **Overlapping readbacks need a RING of bounds buffers.** | Drawing a page needs only texels, which the dispatch writes; the CDLOD split needs the deviation, and the never-split-unmeasured rule already tolerates it arriving late. Removing the readback gate is inherited by `5-4`'s DAG; publishing at dispatch is analytic-era only, because D12's convergence rule says a slot is never sampled mid-erosion. The ring is the load-bearing detail and its absence was SILENT: `generate()` awaits `dispatchWhenReady` before issuing the readback, so the `copyBufferToBuffer` that snapshots the bounds is encoded a microtask later — into the NEXT frame's encoder, after that frame's `boundsBuffer.update()` has re-seeded the atomics. Every page but the first then completed at min `+Infinity`, max `-Infinity`, deviation 0; the selector read zero error and the world converged at the root ring. Measured through the real renderer as 27 nodes and 9 resident pages, with a green Node suite and a green GPU suite. `tests/gpu/terrain-streaming-convergence.test.ts` now drives the whole chain on a real adapter, where it converges to 319 nodes at L2 within 100 frames. |
| `4.5-B2` | The compute meter is fed MEASURED per-dispatch costs, seeded at a measured per-page constant rather than the client's per-frame budget row, and always admits one dispatch for the highest-priority client with demand. | `observeDispatchCostMs` shipped with zero call sites, so every estimate sat at its seed forever and the admission plan was a statement about the budget table rather than about the GPU. The seeds were the whole per-frame ROW, which prices one page bake at everything a frame may spend on page bakes. Measuring them through `timestamp-query` produced the phase's most consequential number: **one height page costs ~1.9 ms of GPU** against a 0.7 ms tier-1 `terrainCompute` row and a 1.55 ms whole-compute cap, so no page can ever be admitted through the normal two-pass plan at any tier. The floor of one is therefore the terrain client's only admission path, not a corner-case net; it lives inside `ComputeBudget` so the `4-0b` invariant survives, and it means the cap can be exceeded by exactly one dispatch (Phase 5's assertion 105 must be authored floor-aware). Same item: ONE `beginFrame` per `TerrainClipmapSystem.update()` — both pumps called it, each wiping the other's plan — and the splat bake, which had never been submitted at all, is now priced with the occlusion bake as one paired admission (assertions 112, 113, 114). |
| `4.5-B3`/`4.5-B4` | Pending pages are re-ranked against the current corridor before slicing; slots stuck in `generating` are reclaimable; an atlas-reshaping `setProfile` reconstructs the generator and both bakes. | `residency.entries` is Map insertion order, which is the order pages were first requested — and `evictionCandidates` considered only `resident` slots, so a request whose dispatch never completed held its slot index forever. The `setProfile` half was worse and entirely silent: an atlas-reshaping quality switch disposed both atlases and left the generator and bakes holding the disposed ones, `generate()` early-returned on `!hasTextures` forever, and terrain streaming was dead for the rest of the session. The NullEngine suite cannot see it — under NullEngine the system never constructs a generator at all — so `TerrainClipmapSystemOptions.computeFactory` is the seam assertion 116 drives it through. `4.5-0`'s recorded residual closes here too: rebuilt caster meshes get a re-run readiness sweep, because a `ShadowDepthWrapper` learns about a submesh only through the forward effect's `onEffectCreatedObservable` and nothing creates that effect at `layerMask 0`. |
| `4.5-C1` | Vegetation does not CAST shadows below tier 2 (`vegetationCastsShadows`, a profile datum). | The largest single term in the tier-1 vegetation draw model and the only one no lever §5.3 governs can move: the near band submits every (species, variant, crown/trunk) mesh once per cascade, 148 of 347 modelled draws and 3.85 of the 9.02 modelled ms. Trees keep the shadows they RECEIVE. §5.3's ordered lever list does not contain shadow casting and neither does its "not budget knobs at any tier" list; the governing precedent is D15, which cut a tier-2 cascade specifically to reduce vegetation shadow draws. Measured on the adapter as 445 → 397 draw calls (the model over-predicts because it counts chunks a frustum drops) and, in the same-host control below, GPU p95 12.53 → 10.76 ms at the reference viewport while the terrain drew 47% more triangles. `VEGETATION_DRAW_CEILING` re-pinned 270/360 → 160/200 and `VEGETATION_FRAME_DEBT_RATIO` 5.57/5.01 → 3.28/2.87. |
| `4.5-C2`/`4.5-C3` | The four terrain compute pipelines are pre-warmed behind the load screen against a real page; the ten material-layer syntheses run in a worker; per-pass GPU counters are summed into `RenderDiagnostics` as UNCORRELATED aggregates. | Babylon 9.21 has no async compute-pipeline path — `createComputePipeline` runs synchronously on first dispatch — and three of the four shaders inline the ~750-line height kernel, so unwarmed they land as multi-hundred-millisecond in-frame stalls during the first second of flight. Warming them against the coarsest real page under the spawn compiles every shader on valid job data and leaves the aircraft over ground that exists. `synthesizeSurfaceMaterial` is ~110 ms of pure CPU pixel maths per layer with no Babylon dependency, so it moved to a worker; keeping that true is why the GPU boundary split into `MaterialArrayUpload.ts`, and consumption stays frame-loop-driven because a `setTimeout` chain was already measured to break startup. The per-pass counters carry no submitted-frame id, so `B-0`'s rule stands and nothing infers a present wait from them — what they buy is that the ~40-70 ms interval against a ~10 ms GPU p95 is inspectable rather than a gap nobody can name. |
| `5-0`/`5-1` | The evolution domain is one world-origin-anchored 1024² grid at 512 m/texel with an open rim and 16-texel analytic blend; exported flow accumulation is upstream area in m². | A moving window would make lake spills depend on flight path. Area rather than a tier- or level-relative texel count gives the hydraulic law one physical input across macro and page exports. Results are same-device bit-reproducible; cross-device identity is deliberately not promised. |
| `5-0` X5 | One invariant splat-id texture plus two seasonal weight textures replace the two complete seasonal id/weight pairs. | `4.5-A2` already consumed ids from the low bucket only, so `splatIdHi` was written and allocated but never sampled. The channel atlas now has six live RGBA8 textures, saving 4 B/texel and one fragment sampler before Phase 5's aux channels arrive. |
| `5-3`/`5-4` | The production integration uses deterministic CPU workers behind the final producer boundaries; the page atlas keeps exactly one erosion page in flight. | This is the correctness reference, not a claim that the plan's final GPU erosion pass shipped. Macro evolution samples the 1024² cell-centred uplift off the main thread. Page evolution uses a fixed 64-texel halo and seeds height/accumulation directly from the canonical macro authority plus band-limited detail, rather than depending on a resident-parent arrival chain. One-page admission prevents stale flight-path work from filling the worker queue. |
| `5-6` | The production page path zeroes erosion across the complete runway earthworks mask and supplies perimeter-drain receiver overrides. | `generateTerrainErodedPage` constructs the overrides deterministically; every override is strictly downhill and the resulting receiver graph is acyclic. Focused tests pin the mask, diversion legality and deterministic regeneration. |
| `5-5` | Four invariant hydrology resources share the page lifecycle but not a GPU format. | Flow accumulation (`r16float`), lake depth (`r16float`), soil depth (`r8unorm`) and signed shore distance (`r16sint`) are uploaded and made resident atomically with the height page, then the aux page is published. Flow/TWI is the current shader-classifier consumer and signed shore distance feeds detail's riparian density; lake and soil remain exposed for later water/ecology consumers, not silently re-derived. |
| `5-7`/`5-8a` | The activated uplift is a seeded broad range/convergence field with rotating double-angle fabric, lithology and two fine bands. | It is not yet the specified Lloyd-relaxed plate-cell/motion-boundary/rift/hotspot model. Fine bands are fabric/lithology/local-rock masked on uplift input but do not yet apply the planned post-erosion soil-depth and curvature mask. |
| `5-10` | Eroded bathymetry currently samples the canonical macro height, cell-centred and blended across the 16-texel rim; it does not overlay resident L0 pages yet. | One uploaded read-only macro buffer keeps the two 1024² R16F clipmaps deterministic while camera motion remains toroidal strip updates. Analytic worlds ignore an installed macro and retain the historical kernel exactly. Replacing the macro sampler with the current L0 authority is a later activation refinement, not something water consumers may implement independently. |
| `5-10` startup | A startup-critical bathymetry dispatch is bounded and abortable at both compute-owner and renderer-owner scopes. | WGSL's reserved identifier `target` made Babylon stop readiness polling while leaving `dispatchWhenReady` pending, so renderer creation could hang without an error. The shader uses `targetTexel`, real-adapter coverage compiles it, `dispatchBathymetryComputeWhenReady` rejects compile/timeout/abort/disposal paths, and `awaitRendererStartup` owns the outer deadline. Never put Babylon's resolve-only helper directly on a startup critical path. |
| `5-A` compatibility | `worldEvolution: "eroded"` is the default, while explicit `"analytic"` worlds retain the historical carve proxies and downhill tracer. | Deleting those paths would break the promised compatibility mode. The eroded runtime does not call the tracer: it extracts one channel graph from the canonical macro export and gives that graph to inland-water presentation. |
| 60 fps push (`6e13d6e`, recorded retroactively) | Near and mid tree crowns are CLOSED OPAQUE HULLS (80-tri icosphere broadleaf / stacked-cone conifer, `DETAIL_OPAQUE_CROWN`), not the 2-12 alpha-card composition; the far impostor bakes from the hull. | The change shipped unrecorded on 2026-08-25 and superseded the 2-12/2-12b card-crown rows for the near/mid bands: the alpha-test overdraw was the measured near-ground frame bottleneck, and the opaque early-Z hull is what carried the tier-1 contract to ~120 fps. The card machinery, five leaf/needle atlas layers and two-sided crown materials remain live for the fix-pack's silhouette fringe below. |
| Fix-pack F1–F4 (2026-08-25) | The hull look is repaired IN the opaque-crown architecture: world-locked cluster noise perturbs the shading normal and modulates tone in the fragment; per-instance azimuth-harmonic wobble (x/z scaling — watertight by construction) plus build-time skirt/hull jitter break the clone silhouettes; reduced-amplitude flutter reaches opaque crowns; dense layers re-authored to ±0.2 contrast; a NEAR-band-only alpha-card fringe (8/6 cards) rides the previously dormant card materials over the depth-filling hull; ground cover moves to 1.9 m candidates / 0.25 share with a near-camera acceptance boost bounded by the 2-16 integral's coverage≤1 worst case. | The user's flight test: "trees look like plastic/playdough". Measured on the reference host: the full fringe at 12/10 cards × 0.30–0.46 halfWidth cost ~4 ms of p95 (two-sided alpha fragments outside the hull's early-Z shadow); the shipped 8/6 × 0.26–0.38 restores ~120 fps on every forest shot. The presentation-build snapshot and the 2-16 budget assertion re-pinned in the same change. Close-review corrections: the ground-cover spacing must DIVIDE the 512 m cell (1.6 = 512/320 — a non-divisor left a bare stripe along every cell edge), the cluster field is world-locked through a `detailWorldOrigin` uniform and fades per-octave on its own pixel footprint, and the fringe mirrors the hull's variant aspect frame. Accepted residual: during the autumn shed window the fringe cards dissolve by leaf fraction while the hull contracts geometrically, so a partially-shed broadleaf can carry a sparse card halo just outside its contracted hull. |
| Fix-pack T1–T8 (2026-08-25) | The terrain meso band exists: two rotated octaves (71/23 m) + altitude-keyed strata (steep-gated, along-strike broken) supply normal/tone/roughness structure between the material tile and the kilometre wash, ALU-only. The micro fade re-keys to the anisotropy-limited MINOR footprint axis and widens 0.5→2 m to 1.5→10 m; the aniso clamp matches the sampler at 16. CDLOD's vertical distance term measures against the slot's MEASURED height interval, not sea level. Black-line fixes: back-face shadow casting, continuous biplanar weakest-plane subtraction, rise floor 0.15→0.32 plus a geometric-hemisphere clamp on the composed normal, corner-morph incident sets forced to 0 when any participant's parent page is missing, jittered horizon-shadow terminator, and deferred (4-frame) GPU disposals for the material-array swap. | The user's flight test: "mountains too smooth, occasionally break into black lines". The 0.5→2 m major-axis fade converged every patterned channel to a flat constant within a few hundred metres of flight-geometry slant range, and NOTHING varied between the 8.9 m tile and the 176 m wash; nodes under a 2,500 m peak could never come closer than 2,500 m to the camera, so high terrain under-split exactly where the report looks. Measured after: p95 9.1–9.8 ms across the terrain shots (baseline 9.0–9.5), +1.6% triangles from the elevation-aware split. LOW_FREQUENCY_KEEP 0.28→0.32 (0.38 broke the Rock anti-moiré ceiling at 0.0342 vs 0.033). Two accepted residuals from the adversarial close review: back-face casting halves which alpha-CARD casters contribute depth at tiers ≥2 (vegetation casts nothing below tier 2, and a canopy's card union still shadows — revisit with the tier-2 shadow work), and the corner-morph parent-missing rule closes only SAME-LEVEL corner sets — a mixed-level corner losing its fine participant's parent still opens for the frames residency lags, because morphing without the parent's texels is not possible at all. |
| Fix-pack W1–W5 (2026-08-25) | `WATER_CAPILLARY_DETAIL_WGSL` (shared, WaterShaders-owned) adds two wind-advected world-locked ripple octaves (~0.42/0.16 m) with pixel-footprint Nyquist fades AND the unresolved mean-square slope of everything below them, folded into GGX roughness by both ocean and hydrology; hydrology re-evaluates its three-sine wave gradient PER FRAGMENT (the lake centre-fan had near-constant interpolated normals). A `water-25ft` capture shot is APPENDED as the first near-water gate. | The user's flight test: "water great at distance, plastic up close". Structural cause: at mip 0 the moment texture holds exactly s², so the 2-8 Toksvig variance was identically zero near the camera — roughness collapsed to the floor and the probe reflected at mip 0 (glass) — while distance recovered real variance through the mips (the glitter the user liked). The tail term is the part of the spectrum no cascade at any tier ever resolves, so folding it unconditionally is correct at every range. Ocean WGSL SHA re-pinned per the 2-8a deliberate-change flow. Measured: p95 ≤9.2 ms on the water shots. |
| Polish pass (2026-08-26) — jet identity | The jet is the **Vesper J-45** again, byte-identical to its pre-fix-pack sim definition and visual build, at the user's request — while the A3/A4 feel fixes stay and are re-derived for its coefficients: SAS yaw-damper gain 1.1 / roll 0.12 (measured closed-loop dutch-roll ζ 0.487/0.623/0.675 at 120/200/260 m/s vs 0.163 open; the F-22 gain 0.20 fails all three floors on this airframe), rate-led pitch-retention capture unchanged and re-proven discriminating on the J-45. The wave-drag machinery stays inert (`transonicOnsetMach: Infinity` on both aircraft) for the next airframe that needs it; `LoftSection.squareness` and `panelStrength` remain as bit-identical-by-default capabilities. The chase camera keeps the speed response re-sized for the 11 m airframe (base 14.3 m + up to +8 m, aimAhead 16 + up to +14) with a look-ahead-smoothed terrain clamp fed by the CONSUMER terrain sample (the analytic sampler clamped against ridges erosion had removed). | User: "change the fighter jet back to the original jet." The feel complaints the F-22 work fixed (release bounce, dutch-roll wallow) were kept fixed — reverting identity does not revert physics repairs. |
| Polish pass (2026-08-26) — the three flight-test reports | **(1) Disappearing trees:** stale-but-visible replaces fail-closed chunk suppression — a >96 m-drifted chunk keeps rendering (fragment band fades use the LIVE camera) while its rebuild streams, suppression only past a 768 m backstop; in-flight builds are never canceled for drift (publish-then-re-dirty converges instead of livelocking); rebuild targets select nearest-first; terrain L0 page publication marks overlapping cells `invalidated` and keeps serving them until the regenerated cell replaces them (the season-epoch pattern, page-scoped — the delete path punched 512 m holes near the aircraft). The near-band crown fringe gets band code 3: near vertex cull plus an 80 m fragment dither dissolve (it popped off whole in one frame at 270 m). **(2) Sudden jerks at good fps:** the floating-origin rebase no longer sets `cameraCut` — the camera rig translates by the origin delta, so the smoother's ~v/7 (≈28 m at 200 m/s) tracking lag is never snapped in one frame every 4,096 m flown; detail publication is STAGED — at most one clone+resetDrawCache per update, on disabled never-rendered meshes (the 4.5-0-safe window), streamed uploads ≤256 KiB/frame, then a one-frame atomic flip (measured: page-thrash-turn zero frames over 16.67 ms, banked-turn one in 240). **(3) Dark/reflective distant trees:** the impostor bake's double-sided orientation test was INVERTED — 0.0% of baked normals faced the bake camera, far diffuse ran 4.5–7.5× darker than the identical mid hull and the unchanged environment specular read as a view-locked sheen; fixed with the comparison flipped, the normal array coverage-dilated (mips no longer average encoded-black — the depth alpha lane was unread and now carries coverage), the billboard base normal camera-facing instead of world-Z, impostor roughness matched to the hull (0.90), and the wrap-transmission term ported to the impostor branch. The fringe's occlusion is scaled by the hull's analytic ramp so near trees stop reading lighter than mid. A normal-orientation + mip-survival assertion closes the test hole (the albedo-only calibration passed at 96–98% throughout). **Layout:** look-ahead 2,400 m so cells generally publish beyond the 2,580 m cull-fade start; newly CREATED batch meshes reveal stochastically over 0.7 s via the previously-unused `detailMeshOffset.w` lane and the vertex kill (no fragment discard — opaque early-Z untouched); re-publications never re-ramp. Six streaming counters (publishedBytes, createdBatches, reboundBatches, revealRampsStarted, suppressedChunks, staleVisibleChunks) flow into the capture's detail-frame correlation. | The three 2026-08-26 flight-test reports, root-caused by a three-lane forensic pass and verified: 17-shot promoted baseline at min 120.6 fps / worst p95 9.4 ms / zero >27.4 ms with everything live. |
| Vegetation overhaul V (2026-08-27) | A "Beta: Terrain Viewer" start-screen mode: a free-fly camera with no aircraft, driven by a synthetic `FlightVisualState` whose position IS the camera. | `src/game/freeFly.ts` owns the rig (WASD/mouse-look/vertical fly, ground clamp via the consumer authority through `FlightRenderer.sampleGroundHeight`); the renderer gains camera mode `"freefly"` (direct rig — response 1, bank follow 0) and `setViewerMode` (aircraft root disabled). Because streaming, the floating origin and shading are all `state.position`-driven, feeding the synthetic state through `render()` makes terrain, detail and wildlife follow the viewer with zero changes to their update contracts — the seam is the one the simulation already uses. Esc restores the menu attract exactly. |
| Vegetation overhaul T (2026-08-27) | Trees are skeletal: the opaque hull is INVERTED from the visible surface into the canopy's dark interior core, and the visible tree is bark tubes (trunk + two branch levels) plus a leaf-cluster card shell at near AND mid. | `detail/treeSkeleton.ts` (vegetation-owned) is a Weber & Penn-subset generator — per-segment curvature, pipe-model radii, golden-angle/opposite/whorled placement, tropism, decurrent trunk splits — with ALL RNG in one named stream so near and mid mesh the SAME skeleton and agree on silhouette by construction. Every tree part registers the skeleton envelope as its `radialUnits`, so one world scale maps bark, core and cards exactly. Mid cards are band code 4: a Bayer complement of near's code-3 dissolve at the shared switch edge (margin 160 → 100 m — the near radius shrank below margin+slack, so a pure-near membership no longer exists and the vertex window arbitrates). The law re-derivation pays for ~7× richer near trees with the near radius: 250/350/400/550 → 110/150/170/240 m at 1,500 tris/plant (measured 824–1,376 across seven species; the estimator is part of the authoring loop), mid 340 (measured 130–334), and the far floor now applies THROUGH the mid band (0.045/0.045/0.035/0.035) because shrinking near inverted the distance profile — far impostors held more density than the 0.7–1.1 km ring. Impostors bake the card shell (without it the far band baked only the dark core). Draw ceilings re-pinned 450/600 → 515/675 for the third tree part; tier-1 model 53.3 draws vs 58. Captures: 121 fps sustained, p95 9.8 ms, zero hitches on forest/reference/canopy shots. |
| Vegetation overhaul G (2026-08-27) | Ground cover is a per-frame GPU compute blade field: three camera-centred rings of OPAQUE Bézier ribbons whose parameters are pure functions of world position — no streaming state, no atomics, no counters, no indirect draws (the deliberate v1 rung: every lattice lane writes a blade or a degenerate zero and the vertex stage collapses the dead). | `detail/groundCoverLaw.ts` owns ring densities/spacings and the 20–80 m altitude gate (free in almost all of the flight envelope); `GroundCoverSystem` bakes a camera-snapped CPU domain tile from the SAME consumer authority the camera clamp reads (height at 1 m; classifier grass weight + harmonised ground albedo — Σ weights × `referenceAlbedo` — at 4 m), so blades stand on the rendered surface and WEAR the terrain's own colour: the far fade line has nothing to reveal. Water/beach/runway excluded at the bake. Opaque blades, not alpha cards, because discard defeats TBDR hidden-surface removal — structurally cheaper per pixel on Apple GPUs. Two landmines cost the bring-up day and are pinned in comments: (1) packed u32 fields MUST travel as uint32x4 vertex attributes — bit patterns routed through float attributes hit NaN canonicalization in the fetch (roots arrived intact, every packed lane arrived scrambled); (2) the material plugin must force `INSTANCES`/`THIN_INSTANCES` off — the 2-12-close `forcedInstanceCount` discovery, hit again verbatim. Budget rows: `groundCoverMiB` per tier (tier 0 funded from the other-detail allowance, measured 1.286 MiB actual against its 8 MiB row). `ground-2m-lowsun`: 121 fps, p95 9.3 ms, zero hitches with the field live. |
| Vegetation/terrain wave Q (2026-08-28) | Three viewer-reported defects root-caused to mechanisms, not tuning: (1) "plastic ground" — Babylon leaves Fresnel F90 at white, so a low sun ran 2–6% dielectrics to full mirror at grazing; the plugin now injects `terrainSurfaceF90` (roughness-suppressed, cap 0.5) at the reflectance anchor, distant roughness converges to each material's ROUGH band end (the Toksvig argument — the midpoint under a flattened normal was the false-highlight tell), the meso band fades on a 4×-anisotropy-credited footprint, and the macro wash gained a dry-warm/lush-cool chromatic swing. (2) The dusk tree cliff at ~1 km — the mid→far handoff stacked four lighting asymmetries; the far band now samples the CSM depth map through a hand-packed fragment-side receiver (zero extra fragment inputs — the 16-input limit stands), with sky-tilted normals, matched wrap transmission, and hull specular parity. (3) "Reptile mountains" — the Rock tile's bedding band family held 40% of its spectral energy in ONE 0.52 m line and the de-tile warp never touched Y while cliffs use Y as V; bedding is now patch-masked/softened (peak power 0.090 → 0.0004), two-scale angular mineral facets replace the weave, a vertical warp octave breaks altitude registration, and the strata noise is two-octave and rotated off the world axes. | Land-cover classification now extends to coarse pages: confidence is LINEAR in log2(texel) (one fifth per level, zero at 128 m texels) and the shader converts it to a cover-noise-mottled class STRENGTH feathered toward the fallback's own composition (grass + the fragment-derived third candidate), with the fallback's alpine/snow terms scaled by the class COMPLEMENT — every page-level border is a ~0.2-strength step dissolved into ecotone mottling instead of the razor-straight polygon edge the first binary widening painted. Two permanent gate shots landed: `hills-dusk-glint` (grazing sun + band handoff) and `mountain-close` (steep-face locate predicate). Gate evidence: forest-500ft's 2.8× per-stem brightness step is a flat ±6 luminance across the same rows; zero cyan/glint terms at dusk; all suites green. |
| Vegetation/terrain/water wave R (2026-08-28) | Three "inherently broken" reports, each traced past its symptom to an authoring or convention bug: (1) TREES — the persistent handoff line was tier 1 collapsing near/mid trees to prototype FAMILIES while the far band stayed per-species (birch drew broadleafOak below the ring and broadleafBirch above it: +28% luminance, view-independent); the impostor now routes through the same `treePrototypeMode` datum, the bake composites the MID budget it actually replaces (impostor/geometry albedo ratio 0.87-0.95 → 1.000 across species), and the normal atlas stores the geometry's own dome/hull/tube vertex normals with per-part runtime sidedness, deleting the view-locked billboard mix and sky tilt (worst-case directional step was −46% at sun-opposed bearings). The manual CSM receiver gained the generator's depth bias, a 0.3 m base lift, and the cascade blend. (2) MOUNTAINS — `smoothstep`'s clamped denominator turns REVERSED-argument calls into degenerate hard steps: Rock's joint crease was an 84%-coverage half-plane (the black/brown/white camo), and Gravel's stone-relief supports were disjoint — literally zero stones (the grey stipple). All ten reversed sites across seven materials fixed, materials rebalanced in-family (cavity medians 0.545-0.686, Rock albedo p99/p01 3.21 → 1.70), and the helper now throws on reversed pairs plus a source-scan test. The wave-Q feather also re-targeted: it faded coarse pages toward GRASS, repainting distant mountains green — a coarse texel's PRIMARY id is never wrong, only its sub-texel mixture, so the fade target is the page's own primary (strictly cheaper: drops a sample). Slope-rock carries the class complement; Rock/Snow height-blend depth widens with reference-albedo distance. (3) WATER — the FFT normalisation used the plain 1/N inverse-DFT convention with no spectral cell measure: the shipped sea's near cascade peaked at 0.14 MILLIMETRES (fp16 subnormals), a mirror wearing normal maps. `√(dk/√2)` per axis restores Hs 0.9-2.6 m at 3.5-11 m/s, strictly kinder to fp16. On top: T2's anisotropy-credited footprint mirrored onto the water detail fades (octave reach 14 → 63 m), roughness became a FIELD (gust patches, slope-coupled, caps unpinned), a mesh-Nyquist term keeps cascades out of vertices the lattice cannot carry (the camera-locked 192-spoke corrugation that read as "plastic tubes"), the lattice snaps to its own step, near rings densified as profile data, foam made reachable with an ocean shore band, and near-only glint jitter on the sun lobe. | Verification: three new permanent gate shots (forest-line-highsun at the user's exact sun geometry, cliff-60m, water-3m with the sun ahead), one sanctioned rebaseline with every frame reviewed, strict tier-1 gate green on all 24 shots (max frame 9.4-19.5 ms), 873 Node + 53 GPU tests, boundaries test's grandfathered tier-reader list now EMPTY (the ocean lattice moved onto WebGpuQualityProfile). Recorded open items: planar shore reflection for the low-altitude band (the last "plastic" term at eye level), hydrology lake/river geometry (fans and 512 m ribbons), DryGrass metre-scale absolute power doubled by its mask fix (sward retune filed), vegetation horizon-shadow term. A ~460 ms single-frame stall in full capture runs (frame ~95 of motion-banked-turn, sync render 4 ms — a main-thread block outside render coinciding with batch creation) appeared twice on 2026-08-28 and has not recurred since the per-frame snapshot allocation was removed; watched, not closed. |
| Fix-pack A1–A5 (2026-08-25) | The jet IS an F-22 Raptor, sim and visual together: real-class mass/wing/inertia/thrust with a Mach-keyed transonic drag rise (trainer's polar bit-identical `base + 0`); gear and contact points from the airframe's real geometry; `LoftSection.squareness` (superellipse) gives the chined flat fuselage; twin 28°-canted fins, all-moving stabilators on the elevator node, clipped-diamond wing, single-seat gold canopy, twin rectangular nozzles; `panelStrength` recipe datum turns the 64² panel grid down on the 19 m airframe. Release-bounce: DirectPitchRetention captures a rate-LED settle target (τ 0.35 s) and ramps hold authority over ~0.5 s. Dutch roll: a washout yaw damper + roll-rate damper for the jet on pilot-neutral axes only (the DirectPitchRetention doctrine precedent), closed-loop ζ 0.47–0.73 vs 0.11 open. Chase camera gains a speed-driven distance/aim-ahead/FOV response for the jet; per-kind cockpit eye. | The user's flight test: "change the fighter jet to an F-22; the jet should appear further in the distance at speed; bounce on release; feels drunk". The bounce was the attitude captured at the release instant while the airframe still carried pitch rate (input releases at 2.8/s, actuator 7/s); the wallow was dutch roll at ζ≈0.11 coupling into roll through Clβ with zero augmentation in the default mode. Wave drag is required physics, not tuning: without it the F119 thrust drove 480+ m/s at sea level. Max level speeds land at 363 m/s SL / 436 m/s at 10 km. Behavioural jet tests re-pinned deliberately (thrust closed-form, cruise band, spawn/landing geometry); trainer suites pass unchanged. |
| Phase 6 Gate 0 (2026-08-30) | The capture gates now protect TODAY'S delivery, not the 60 fps contract floor: per-shot wall-fps/p95 floors and drawCall ceilings re-pinned for all 24 shots from three clean idle-host runs (`PerfCaptureShotCeilings` docblock carries the rules; floors move only at PHASE_6_EXECUTION_PLAN.md §9 rebaseline points by recorded decision), and a HARD capture assert on `inventoriedGpuMemoryMiB` at 495 MiB (the gating estimate reads ~381 MiB while the Babylon inventory reads 489.0 at reference-viewport — the estimate-only gate was ~100 MiB of false headroom; 6-11.4 reconciles and ratchets). The eroded world is reachable by URL opt-in only (`?world=eroded`, never persisted, shipped default pinned analytic by test); the PR-CI subset carries the three water shots for the phase. Constraint: any pixel- or delivery-intent change now requires the §9 rebaseline flow to re-pin floors in the same promotion. |
| Gate W measurements before the port (2026-08-30) | Three committed instruments replaced plan-era guesses: `scripts/erosion-benchmark.mts` (macro per-operator wall, fingerprint-locked to the shipped pipeline: talus 1,851 ms dominated; the plan-quoted ~470 ms lakes tail was contention noise — 58 ms real), `scripts/twi-stats.mts` (TWI over eroded L0 pages: real range ~13.3–29, median ≈18 — the shipped [4,18] window had an empty dry half and saturated ~half of all land; re-pinned [15,24], eroded-only, analytic classifier path unaffected), and `scripts/erosion-seam-audit.mts` (W-8/C-10: 36/36 adjacent production page pairs IEEE-bit-exact on stored overlaps across levels and flow regimes). Constraint: C-10 is closed as a MEASURED guard — the audit re-runs at every Gate W boundary and before any re-default, with the adversarial production-ratio fixture pinned in `tests/render.webgpu-erosion-seams.test.ts`; the halo stays 64 and growing it to 80 is the recorded fallback if either instrument fails. |
| W-6 bathymetry L0 overlay (2026-08-30) | Eroded bathymetry now overlays RESIDENT L0 eroded pages inside `BATHYMETRY_UPDATE_WGSL` (page-table storage buffer + height-atlas `textureLoad`, macro-facing-border feather capped at 32 m; internal resident-resident seams keep weight 1 because stored gutters agree bit-exactly), invalidated by residency deltas coalesced into per-page strip rects (~0.08% of a full-square refresh per delta). The consumer surface (`BathymetryShaderBinding`, `WATER_DEPTH_OPTICS_WGSL`, `waterDepthFromBathymetry`) is byte-unchanged and `sampleBathymetryTerrainAuthority` STAYS the macro floor with its divergence documented. Root-cause fix shipped with it: `StorageBuffer.update` is `queue.writeBuffer` (pre-submit), so two same-frame update dispatches read the LAST rect's params — the pre-existing dual-strip path latently raced; per-dispatch short-lived buffers close it. Constraint: eroded waterlines now move with L0 residency; W-7's eroded captures pin that behaviour, analytic shots provably inert (empty-table sentinel). |
| W-1 GPU-erosion hybrid, macro leg (2026-08-30) | The macro evolution is a staged hybrid behind the same client boundary: worker stage 1 (sampling + flood + MFD) → main-thread GPU stream-power/talus (W-1a: gather-only ping-pong WGSL, 2,020 ms → ~100 ms, GPU-vs-GPU byte-deterministic, CPU-oracle tolerance pinned measured-not-conceded) → worker stage 2 (`finishMacroEvolutionFromEvolvedHeight` — W-1c extracted the shared completion half so the two entry points cannot drift; the zero-iteration duplicate flood+MFD pair is gone, 1,844 → 324 ms). W-1c also rewrote the CPU legs bit-identically (typed-array heap, radix ordering with the identical permutation, in-band trace markers): floods 476→151 ms, MFDs 477→170 ms, with full-field byte A/Bs against the pre-change module at 1024² on two seeds. W-1b added GPU twins of the uplift/geology samplers (29.9 ms vs 1,023 ms; fabric-frame atan2 conditioning at codimension-2 zeros is the one pinned lossy leg — isolated metre-scale ridge-phase cells, means sub-millimetre; 6-seed extraction sweep green with pinned population-drift bands). Constraints: the single-shot CPU worker path stays byte-identical (fingerprint-pinned) as oracle and fallback; hybrid provenance is a distinct same-device family (`gpu-macro-v1/*`) — nothing may pin eroded fingerprints across CPU/hybrid families; assertions 91/93 are now written and green (`tests/gpu/terrain-collision-readback.test.ts`, `tests/sim.flight-authority.test.ts`). |
| W-5 eroded water geometry (2026-08-30) | Lake covers and rivers are real geometry: marching-squares 0.5-contour shorelines on 128 m coverage grids (Douglas-Peucker simplified, ear-clipped, area-gated graded interior refine), arc-length river stations (`clamp(width*2, 32, 256)` m) with bit-exact node hydraulics and a world-anchored arc-length uv for 6-1's advection. Lakes meshed 2,654 → 9,173 (the convex-hull overfill gate is retired WITH its reason — the contour of bilinear coverage cannot overfill its mask; the wet-support and area floors stay). Legacy analytic builders byte-untouched and sha-pinned. Constraints: lake vertices sit EXACTLY at spill elevation (the 0.05 m planar-reflection matcher); islands are a recorded residual (the polygon contract stays single-ring); the ~+29 MB eroded-only mesh memory and the extract/mesh startup cost are routed to W-1e and the §8 re-default checklist. |
| W-1e eroded startup + graph relocation (2026-08-30) | Channel-graph extraction moved INTO the macro worker (new `channel-graph` staged event posted after `complete`, polygon buffers transferred; the macro fields are COPIED not transferred on the complete event because the extractor still reads them), and `FlightRenderer` awaits the graph immediately before `HydrologySystem.create` instead of inside `TerrainEvolutionRuntime.initialize` — so bathymetry, aircraft, airport, detail, ground cover and wildlife construction now overlap extraction. Readiness semantics are UNCHANGED: hydrology is still fully built before startup resolves. Extraction itself is 3.9x faster (marching squares on typed arrays with edge-derived crossing points, exact-count active-cell lists, per-node coverage memo: 1,016 → 246 ms) and mesh assembly 1.8x (399 ms). Constraints: any producer that does not advertise `channelGraphFollows` keeps main-thread extraction, and a worker graph that fails validation falls back to the in-process extractor — startup can never strand on it; `scripts/channel-extract-benchmark.mts` carries per-leg timings AND graph/mesh fingerprints so every future A/B is self-proving. |
| W-1b sampling wired into the macro hybrid (2026-08-30) | Macro input sampling runs on device (`evolve-stage1-presampled` transfers the three sampled fields in, and the worker's stage-1 head runs the identical reference derivation), measured 1,011 ms CPU → 28.8 ms GPU. The "eager worker start" it replaced overlapped only 13.9 ms of synchronous construction — measured, not assumed — so presampling is the unconditional default when the port is present (crossover would need a 1.03 s synchronous block, a 74x margin). Sampling fails OPEN to the worker's CPU pass (an exact twin is one command away; a failure costs ~1 s of latency) while the stream-power/talus leg stays fail-CLOSED (no twin on that path, so a failure there is a defect that must surface). Constraint: GPU-sampled worlds are a DISTINCT same-device landscape family — provenance `gpu-macro-v2/<adapter>` vs `gpu-macro-v1` for CPU-sampled hybrid — because inputs agree only to tolerance (mean 4.1e-4 m) and the drainage solve amplifies that into different receivers at f32 ties. Nothing may pin eroded fingerprints across families. Macro leg end to end: ~1,779 → ~825 ms. |
| W-7 landscape statistics (2026-08-30) | The five long-allocated landscape-quality assertions have instruments at last (`tests/world.evolution-stats.test.ts`, `tests/gpu/terrain-transect-spectrum.test.ts`). PASSING and now pinned: 87 domain-wide macro pit density 0.0203/km² (target < 0.1); 88 lake spill == flood fill surface, 13,759/13,759 BIT-exact, zero page-wet texels outside the macro lake field, page lake surface within half a quantization step; 96 global gradient-orientation band 1.1716:1 (target < 1.3, only 11% margin — a macro-shape change can move it); 97 no spectral cliff above the 4 m Nyquist limit, the historical 43 m cliff gone, octave power monotone from 128 m to 4 m. RECORDED-not-pinned (real measurements carrying `// W-4 target:`): 87 at the named 50 m footprint 3.289/km² over 19 real L0 pages (ridge 5.36, valley 0.00); 96 local, 76% of ranges reach 2:1 (median 2.430:1); 98 valley:crest 20 m RMS curvature 0.608:1 against a ≥ 3:1 target — INVERTED, not merely short. Constraints: assertion 87's literal wording named an instrument nobody designed (50 m over 524 km is 1.1e11 samples and no 50 m authority exists), so it is split into a domain-wide 512 m half and a page-footprint half, documented in-test; assertion 96 measures gradient ORIENTATION (compass bearing, axial mod 180°), not slope angle; assertion 97's spectrum uses a per-bin MEDIAN across transects because three of sixty transects crossing a shore or talus break move the mean by two orders of magnitude. |
| Gate W adversarial review (2026-08-30) | Six finders over the landed Gate W work produced 16 candidate defects; a refutation panel killed 3 and confirmed 4, and the two that mattered were instrument failures rather than feature bugs. (1) The capture's inventoried-memory wall was BLIND to storage buffers — `inventoryGpuMemoryMiB` walks `scene.textures` and mesh geometry, and every allocation Phase 6 adds is a `StorageBuffer`, so the assert read byte-identical no matter how much scratch landed and a `DYNAMIC_ALLOCATIONS` reconciliation would have recorded 0.0 MiB. `core/GpuBufferInventory.ts` now feeds the inventory floor, allocations register their bytes, and a source scan over every `new StorageBuffer(` site keeps the blindness from returning; pre-existing sites are allowlisted with reasons and an asserted list length, because registering them RAISES the reading the 495 MiB ceiling was pinned against and must land with a fresh idle-host re-pin. (2) The sampling leg's documented fail-open was unreachable: Babylon's `dispatchWhenReady` never settles when a pipeline fails to compile, so a rejected sampler WGSL would hang the load to the 180 s evolution timeout instead of falling back to the CPU twin — the terrain producer now polls with a deadline and rejects, matching the water stack's bounded-dispatch helper. (3) The bathymetry overlay's per-delta rect path had no cap and never used its own full-square fallback; it now promotes past a batch limit. Constraint recorded with the fourth finding: **capture SSIM only reaches 1.0000 on an idle host** — under load an unchanged tree drifts to ~0.99 luma / ~0.93 worst-tile, inside the gates, so any close proof or "analytic pixels unchanged" claim taken on a busy machine is vacuous. The cheap disambiguator is a worktree control run at the last known-good commit under the same load. |
| W-1d + W-2 GPU page-erosion DAG (2026-08-30) | The steady-state page path is a multi-frame GPU DAG behind the unchanged client boundary: SEED (analytic kernel + uplift + macro/parent composition) → BREACH (orderable-f32 `atomicMin` carving, order-independent because min-combine is commutative, associative and idempotent) → MFD in the worker (the CPU reference, unchanged) → STREAM POWER and TALUS on W-1a's ping-pong shaders → FINISH in the worker (the existing hydrology/blend/stats path). Measured 34–37 ms/page across 155 dispatches, 12–19 frames end to end including the L0 collision readback, ≈1.4 pages/s at 60 Hz against the CPU worker's 0.2–0.5 — and `COMPUTE_DISPATCH_SEED_COST_MS.erosionCompute` is now a MEASURED 0.24 ms rather than the placeholder 0.4. Talus is 55% of the page and is the next lever. Four forced deviations, each with its reason recorded in-file: banding selects by `workgroup_id.z` so one param write serves many bands (the draft's per-frame cursor capped a page at ~96 frames); six 384² scratch fields, not ten, because tier 0 has 0.37 MiB of headroom at its largest viewport and a seventh field is 0.56 MiB — so geology runs twice (+1.3 ms) rather than staying resident; parent-seeded depth is capped at ONE level because each chained level adds a 2×2 seed block per page to the required-resident set (~100 extra slots against a 144-slot atlas at nine deep); and the W-2 admission gate reads `hydrologyReady` rather than channel residency, because a channel slot stays `generating` until its occlusion and splat bakes land and neither writes the log-flow texels the seed reads. Deadlock-freedom is by induction: dependencies always name strictly coarser levels, the chain terminates at the first macro-seeded level, and the gate `continue`s past a blocked candidate rather than returning — pinned by a test that refuses every level-1 page forever and asserts the coarse ladder still streams. GPU-vs-GPU determinism holds across evict/regenerate AND across three different admitted-dispatch rates, so a page's bytes do not depend on how the meter paced its DAG. |
| Assertion 90 relaxed for the GPU producer (2026-08-30) | Adjacent GPU pages are NOT bit-equal on their stored overlap: measured ~8 f32 ulps (1.22e-4 m E-W, 9.54e-5 m N-S over 2,112 texels/axis), because the WGSL kernels take PAGE-RELATIVE split-origin lattice coordinates — the same world-scale precision rule that keeps f32 honest at ±262 km — so two pages decompose a shared world texel differently and round differently. A CPU control on the identical fixture is bit-exact, which separates this cleanly from W-8's composed-reach question. Accepted for Gate W and pinned as `TERRAIN_PAGE_EROSION_GPU_SEAM_CRITERIA`, with the constraint recorded that the bound is ALTITUDE-DEPENDENT: 8 ulps is 0.008 mm at 10 m but 1.95 mm at the 2,400 m terrain ceiling, so the pin is the ulp ceiling itself and sits only ~2.5× under the 5 mm physics tolerance. The fix is known and required before the eroded world is ever re-defaulted (§8 criterion 6): snap the lattice origin to the 512 m world block containing each texel instead of to the evaluating page, using the kernel's existing page-uniform ARRAY and runtime selector — a page's 768 m scratch touches at most 3×3 blocks. Until then the CPU producer is the bit-exact oracle. |
| W-4 plates + post-erosion bands, and the disproved diagnosis (2026-08-30) | The fine 24 m/9 m ridged composite moved off the uplift input to a post-erosion mask keyed on soil depth and convergence curvature (`terrainFineBandSurvival`, thresholds measured over the W-7 page spread, not guessed), and the plate model gained Lloyd-relaxed sites with per-plate motion vectors and pair-weighted convergence. Two formulations were tried and rejected WITH numbers rather than by taste: a nearest-site plate field was discontinuous (0.972 of full scale between adjacent macro texels at triple junctions, vs 0.033 for the shipped pair-weighted form), and rotating the range channel into the boundary's across-strike frame made range-local anisotropy WORSE (median 2.913 → 2.050) because the boundary normal turns per site pair while the 96 km fabric stays coherent in a 16 km window — the dead output was deleted, not left unread. **The register's C-4 diagnosis was disproved by ablation:** deleting the bands entirely moves sub-macro pit density only 3.289 → 2.961/km² and valley:crest curvature not at all, and masked bands at ×1/×2/×4 leave the pit count identical — a 24 m band box-averaged into a 50 m cell cannot make a hollow. Every measured gain (96-global 1.1716 → 1.0661:1, 96-local median 2.430 → 2.913:1, 87-fine → 2.574/km², 98 → 0.805:1) is the plate model's. The two remaining misses are re-routed to their real, measured mechanisms: sub-macro pits are 4–20 cm sills inside 1.2–6.4 m rims and **nothing drains between the page breach's 32 m reach and the macro flood's 512 m**; and a page has **no hillslope domain** because its contributing area is the 512 m macro accumulation upsampled (p1 area at a 2 m texel is 2.9e5 m² — every texel believes it drains 29 hectares), which is W-2's boundary condition, not a band property. Constraint: the analytic surface is bit-identical across this change (`sampleNaturalTerrainHeight` and `sampleTerrainHeight` fingerprints equal on a HEAD worktree), and the CPU-reference sampling leg's +32% is the fallback/oracle path only — the shipping GPU sampler measured 26.2 ms. |
| Gate W CLOSED (2026-08-30) | Close evidence, all on a quarantined idle host with a Wave-1-clean tree (the 6-4 caustics branch is deliberately unmerged): **24/24 capture shots pass with delivery gates ENFORCED** — minimum 112.44 wall fps, worst p95 10.3 ms, **zero hitches** — against the pre-Gate-W floor of 113.4 fps / 10.2 ms / zero, i.e. the whole workstream cost no measurable delivery. The analytic set is SSIM-unchanged: 23 of 24 shots at ≥ 0.999 luma and the single shot below it (`canopy-1200ft`, 0.9984) measured 0.9987 on a control capture of the pre-Gate-W commit under identical idle conditions — inherent foliage-streaming variance, not movement. That control also settles D-6's open question: the W-6 dual-rect race fix has **no detectable analytic pixel effect**. `npm test` 951 passed / 114 files, `npm run test:gpu` 73 passed / 36 files, W-8's seam audit 32/32 bit-exact, and inventoried GPU memory reads 492.3 MiB at the binding shot with every allocation site now registering (previously 489.0 with storage buffers invisible), leaving 2.7 MiB under the pinned ceiling. Carried out of the gate: D-7's canonical-split fix (required before re-default, §8 criterion 6), D-9's loosened CPU/GPU page-parity bounds (to be re-tightened once D-7 lands — a loosened bound that outlives its cause becomes permission), the two W-4 misses routed to reach and to W-2's boundary condition, and Gate F's flights, which still gate any eroded baseline promotion. |
| 6-6 ecology channel consumers (2026-08-30) | `soilDepth` has named consumers for the first time (clutter density and the moss gate through a shared `soilLitterFactor` law in `densityField.ts` — the one sanctioned terrain→detail entry point — plus forest-floor suitability baked in the splat compute and a CPU delivery path), and signed shore distance gained its species/appearance half (reed/fern archetype weight and wet-litter darkening, with the four `RIPARIAN_BANK_*` distances injected rather than restated in each consumer). Phase 5 register row C-9 is therefore half discharged, with `lakeDepth → 6-5` now asserted as the ONLY open row by a consumer-per-channel table test — the plan row's "named consumer or the item produces data nothing reads" rule made executable. Two deviations, both stronger than the brief: GPU consumers are SPLIT BY STAGE (soilDepth into the splat bake compute, because litter is a per-page property and costs zero per-fragment samplers; only shore distance reaches the fragment, as `texture_2d<i32>` via `textureLoad`, so the sampler budget does not move — 9 fragment textures against the 16-per-stage limit, 8 in the shipping analytic build), and the fragment consumer is gated by a COMPILE-TIME DEFINE rather than a runtime zero-sentinel, making it cost-dark as well as pixel-dark. Constraint recorded from a real defect this work exposed: **a Babylon `MaterialPluginBase` define must be listed in the constructor's defines map or its `#ifdef` silently reads false** — the wet-litter block was compiling out of a shader that had its atlas correctly bound, and a `prepareDefines`-only test cannot see it. Only an assertion on the COMPILED shader source catches this class. Evidence standard: analytic byte-identity was measured against a reconstructed pre-6-6 tree (digest `a46e54b1` both sides) rather than asserted, and stem counts fell as §5.3 requires (clutter −6.0%, moss −24.3%, trees and shrubs byte-equal). |
| 6-7 talus/scree placement (2026-08-31) | Rock is now TWO named populations rather than one biome table, and the split fixes a defect in shipped code: 2-15's loose-block term scaled with `slope·0.35` without limit toward vertical, so the steeper a face got the more loose blocks it carried — backwards, because a face above the local angle of repose is a failure face that sheds. `TALUS_FACE_SHED_MAX = 0.55` of that population now leaves the face and lands on an apron placed by `density = rest · supply · runout · exposed · instancedShare · (1 − snowBuried)` (`detail/talusField.ts`, Class P, owned): an upslope fall-line probe integrating metres of ABOVE-repose face, a runout falloff from apex to toe, and a soil-exposure factor whose window `[0.45, 1.9]` m is measured over 5,546 apron-capable sites rather than borrowed from `soilLitterFactor`'s `[0.9, 4.6]` (which would have left the channel decorative). Lithology moves density, grain AND the resting band through ONE owned number, `reposeDegrees`, because repose is itself a statement about clast size and angularity — nothing inverts the producer's unpublished hardness lerp. Grain uses fall sorting (chips arrest early, blocks roll to the toe, so the apron fines UPSLOPE toward its source — the opposite of a water-laid deposit), with the direction pinned by test. Constraints: snow burial keys to the REFERENCE snowline, not `FoliageSeason`, or rocks pop with the calendar; the fall-line probe samples `sampleTerrain` directly rather than the 16 m scatter grid, because the grid clamps past its 40 m halo and a grid-read probe would answer differently depending on which cell was generating — a seam in the scree; and the grain blend reuses the SAME RNG roll as 2-15's size law, so the stream is untouched and a no-apron cell is byte-identical. Net rock instances fall in every measured window (broad −10.9%, mountain −21.4%, flat 0.0% and byte-identical), the mid-radius boulder population falls 13–31%, draws are unchanged by construction, and generation cost rises 0.5%. Carried: the detail field keys geology on `world.sourceSeedHash` while the erosion operators key on `world.seedHash` — a valid but different lithology realisation, worth closing if a terrain-side scree albedo lands. |
| Wave 1 water in motion — 6-1, 6-2, 6-3 (2026-08-31) | Three items sharing one physical chain, each pinned against its closed form rather than tuned. **6-1 (flow advection):** dual-phase Vlachos advection at 9 / 1.6 / 0.36 m drifting at 1.0 / 0.85 / 0.7× surface velocity (large structures ARE the water; small ones are capillary-gravity waves with their own phase velocity), with a bounded Lagrangian age because unbounded `p − v·t` shears the lattice without limit, walks the lattice coordinate past f32 integer exactness, and pops on reset. Standing waves are stationary exactly when `c = v`, so **speed sets wavelength** (λ = 2πv²/g) and **grade sets steepness** (ka = 0.55·gradeNorm, Stokes-limited) — the first formulation made amplitude speed-linear, which made `ka ∝ 1/v` so a torrent broke LESS than a riffle; caught by measurement and re-derived. Lake chop is SMB/JONSWAP fetch-limited growth, with `√(F/F_ref)` in the vertex payload so the rasteriser interpolates wave height rather than fetch. World-locking is proven bit-identical from 0.5 s to 20,000 s while the advected half of the same probes is asserted to move. **6-2 (run-up):** Hunt (1959) with the binding same-cascade rule implemented as ONE named function the GPU test calls, selecting on visible AMPLITUDE not slope. The phase is a shallow-water eikonal in DEPTH (`φ = ω·(t + 2√h/(tanβ·√g))`), whose gradient is exactly `ω/√(gh)` — refraction and shoreward crest narrowing for free, no shore-distance texture. Foam is MEAN-PRESERVING (cycle mean exactly 1), so wave R's tuned coverage is redistributed, never added to. The wet-sand field is terrain-side for a geometric reason: the ocean disk is a plane at sea level with depth-write off, so above the waterline the terrain fragment is nearer and the disk is depth-tested away — the water surface cannot draw the sheet on the beach. **6-3 (shoaling/breaking):** the full `√(c_g0/c_g)` coefficient (it reproduces the shoaling dip Green's law cannot) collapsed to `Ks² = kh/(2n·k₀h)`, and a clipped Rayleigh cap against a slope-dependent McCowan limit that is exact rather than approximate and yields whitewater for free. 6-2's bore and streaks are multiplied by 6-3's breaking fraction, so the two items cannot disagree about the same wave. Constraint recorded from a hardware defect only the GPU test could see: **WGSL `tanh` is lowered to `(e^(2x)−1)/(e^(2x)+1)`, overflows f32 above x ≈ 44 and returns NaN, and `min(NaN, guard)` returns the GUARD** — so a ceiling silently became the value across most of the shelf, invisible to the f64 oracle. Cap transcendental arguments at a value that saturates identically in both precisions (20 for `tanh`) and pin it with a source scan. |
| Erosion readback race fixed (2026-08-31) | The recurring `drainageHeight[0] must be finite` failure in the page-erosion GPU suite was diagnosed as host contention by four separate agents and documented as environmental twice. It was a real race: `runReadbackAndMfd` issued four `StorageBuffer.read()`s through `Promise.all`, and concurrent reads race Babylon's staging/`mapAsync` machinery — a copy not yet submitted when its map resolves returns ZEROS rather than raising. Zero decodes to `~0 >>> 0` = 0xFFFFFFFF = NaN, which then surfaces hundreds of lines away inside the MFD stage, naming a symptom in another subsystem. The fault is detectable because of an arithmetic property of the encoding, now proven by test: `pOrderableEncode` maps a positive float to `bits | 0x80000000` (high bit always set) and a negative one to `~bits` (zero only for the NaN payload 0xFFFFFFFF), so **zero is not a legal encoding of any finite float**. Three changes: reads are SERIALIZED (one queue, same submits, no measurable cost); a faulted readback is RETRIED once, because the GPU buffer still holds the result and the fault is recoverable rather than fatal; and `decodeOrderableFloatBits` refuses a faulted buffer with a typed `TerrainErosionReadbackFaultError` that names the readback rather than the arithmetic. The GPU suite went from intermittently red on a quiet tree to 41 files / 90 tests green. Constraint: never issue concurrent `StorageBuffer.read()`s on this device path, and when an encoding has unreachable values, spend them on detecting transport faults — a guard that converts an impossible value into a named error is the difference between a one-line fix and a four-agent misdirection. |
| Cross-authority channels carry their own authority's seed AND its drivers (2026-08-31, `6-8` fix) | A page channel baked from another subsystem's field must be evaluated with that subsystem's seed and that subsystem's driver values, or it describes a world that is not being drawn. | A world has TWO seeds: `createWorld`'s guaranteed-airport search replaces `world.seedHash` while every plant is placed from `world.sourceSeedHash`. `6-8` appended the vegetation density lattices to the TERRAIN kernel's page uniform, where they inherited the terrain seed — measured 0.008 baked closure against 0.90 standing at `grove-forest-2m` (2 stems/ha against 630/ha), which flipped the ground from forest floor to grass at 51 of 81 probes. `buildTerrainKernelPageUniform` now takes `extraSeedHash` for appended lattices (omitted is byte-identical). The same bake's airport influence was a 240 m disc about the runway centre under a comment claiming the rounded-rectangle SDF, reading 0.000 where `getAirportInfluence` returns 0.807, so the classifier lost its mown-grass decree and the canopy sample lost the apron's stem clearance; `splatAirportInfluence` transliterates the real field to 2.2e-16. A before/after probe that substitutes the SAME wrong input on both sides (D-14's) cannot see either defect. |
| Wave 1–3 baseline rebaselined — R1+R2+R3 (2026-08-31) | Capture `2026-08-31T16-39-53.222Z` reviewed shot by shot and approved: 24/24 with delivery gates enforced (min 117.73 wall fps, worst p95 9.80 ms, zero hitches, zero frames over 27.4 ms, max frame 18.10 ms). The governing rule is D-18's — **a shot that moves against its own item's prediction is a defect to investigate, not a rebaseline to absorb** — and it earned its keep twice. `night`'s worst-tile RGB SSIM of 0.5568 is a FALSE ALARM: that statistic is unstable on near-uniform dark tiles and the shot's mean luminance is 10.2/255, while its whole-frame value is 0.980 and the frames are visually identical. `canopy-1200ft` (+9.7% luminance) and `grove-forest-2m` (−13.0%) moved in OPPOSITE directions, which read as a defect and was first attributed to 6-6 — whose plan line promises an analytic sentinel fallback and whose D-10 records analytic byte-identity. **That attribution was wrong.** The mover is 6-8's `canopyClosure`/`grassCover`, documented in `LandCoverClassifier.ts` as deliberately live in BOTH worlds and carrying no zero-sentinel, because a canopy is a vegetation property rather than an erosion product; 6-6's `soilDepth` is separately gated on `soilDepthValid` and `generation.ts` states the analytic case outright (no soil channel, no `soilDepthMeters`, every downstream number bit-identical). D-10 stands, and the parity test's "channel omitted" probe IS the real analytic case rather than a decorative fixture. The pixel change corrects a defect of D-18's own family: canopy closure now SEPARATES two cases the shipped classifier conflated — litter under closed canopy, grass in glades and open pasture — with `approach-500ft` (flat grey-brown dirt to green pasture) the clearest evidence and the meadow/forest pair the discriminating test, which passes. Waterline movement was verified by row profile rather than by eye: `water-3m` carries 80% of its difference in 35 of 720 rows, a crisp band, as 6-5/6-2 predicted. **Floors deliberately NOT re-pinned despite §1.2's "re-pinned at each":** the documented rule is `floor(min-across-runs × 0.85)` and this is one run on a cool host; the same tree measures ~20% apart cold vs warm, so re-deriving from 117.7–120.2 fps yields 100–102 — indistinguishable from the standing 98–102 pins while sampled entirely from the favourable end of the drift band. Re-pin at R4 from three or more runs. Carried forward as constraints on 6-11 and Phase 7: draw-call slack is a near-uniform +8 across all 24 shots (~5%), and inventoried GPU memory is 492.3 MiB against a 495 ceiling — 0.5% headroom. |
| Sampled-binding budget derived from the compiled shader (2026-08-31, `6-11.4`) | A binding contract must be asserted against the effect the device was handed, never against the declaration sites that are supposed to produce it. | `TERRAIN_SAMPLED_BINDINGS` was checked only for uniqueness and against the per-stage cap — both statements about the LIST, neither about the shader — and was consequently wrong in both directions for some time: it listed six PBR samplers this material never declares and `terrainHeightAtlas` in a stage that reads it by `textureLoad`, while omitting `environmentBrdfSampler` and the shadow sampler a `receiveShadows` mesh compiles in. Measured fragment total is **10**, matching none of the 11 / 8 / 15 that Phase 6 items had quoted from it, and the correct vertex total is **ZERO** (the CDLOD path reconstructs bilinear from four corner `textureLoad`s, so it takes no sampler). The docstring further claimed the material factory asserted the list against `engine.getCaps().maxTexturesImageUnits`; no such assertion existed, and the only occurrence of that capability name in `src/` was inside the claim. `tests/gpu/terrain-sampler-budget.test.ts` now compiles two shipping permutations and pins each set exactly. It also settles 6-5's arithmetic in the stronger direction: **binding both hydrology channels costs ZERO sampled bindings** (`TERRAIN_HYDROLOGY_ADDS_SAMPLED_BINDINGS = 0`), because shore distance is r16sint — an integer texture cannot be filtered and needs no sampler by rule — and lake depth is r16float read at an exact texel. Constraint recorded from a near-miss inside this very fix: the first harness lit the scene with only a `HemisphericLight` and measured 9, because the shipping beauty mesh sets `receiveShadows = true` under a cascaded generator and that permutation compiles a NARROWER shader than ships. **Deriving from the artifact is necessary but not sufficient — the artifact has to be the one that ships**, or a wrong number is merely replaced by a differently wrong one that looks rigorous. The shadow sampler's name carries the light's scene index and is normalised for that reason, and the empty vertex expectation is guarded against vacuity because an empty set passes equally when the stage samples nothing and when it never compiled. |
| A test that drives a subsystem differently from production is not testing production (2026-08-31, Gate F) | The eroded world rendered as flat plates over water — no relief anywhere — after nineteen days of Gate W work that closed green on every instrument it had. Root cause: `TerrainPageErosionGpu.demand()` answers the admission meter with the current DAG stage's remaining dispatches, and `W-4` added a `fine-band` stage to the stage union, to `advance()`, to both ping-pong shaders, to the measured cost row and to the cost trackers — **but not to `demand()`'s switch**, whose `default` returned zero. The rest is silent and total: zero demand → the clipmap submits nothing → `ComputeBudget.admitted` returns 0 for a client that never submitted → `dispatchPageGeneration` returns at its `admitted <= 0` guard → the page is never pumped again → **not one eroded page ever becomes resident**. Measured `resident=0 pending=40` on every eroded shot before, `resident=70/116/168 pending=0` after; a probe driving the DAG through a real `ComputeBudget` sat 1,970 of 2,000 frames in `fine-band` and converges in 31 frames after. **The reason no instrument could see it is the durable lesson:** the GPU harness pumps the producer unconditionally every frame, the renderer pumps only what the meter admits, so `demand()` — the function that failed — was called by NO test in the project. Byte-determinism, seam audits, CPU-oracle parity, statistics and timing were all real and all green, and every one of them drove a pump loop the application does not use. The convenience that makes a harness easy to write (drive the thing directly, ignore the scheduler) is exactly what removes the production contract from test coverage. Fix: the missing case, plus `default:` now failing **OPEN** (one dispatch) rather than **CLOSED** (zero) behind a `never` exhaustiveness check, so a stage added to the DAG is a compile error instead of a flat world — an admission meter that stops asking when it meets an unrecognised stage has its failure direction backwards, and the check caught a second unhandled member (`idle`) at the first compile. Instruments: `tests/gpu/terrain-erosion-live-pump.test.ts` drives the DAG through a real budget as the renderer does and asserts structurally that only async/terminal stages may report zero demand (both tests verified to FAIL on the reintroduced defect); three `eroded-*` capture shots so the eroded world is RENDERED and reviewed, which W-7 left undone; and the capture harness now writes frames BEFORE its gates rather than after, because withholding the images on a failing run is precisely backwards for an instrument — a `STATUS.txt` reading NOT APPROVABLE until every gate passes preserves the property that a failed run's candidate must not look promotable. |
| §8 resolved NO — the eroded world is SHELVED and analytic ships (2026-08-31) | Jason re-flew the eroded world after the Gate F deadlock fix and terminated it for this phase. This is §8's planned branch on its own terms ("the analytic default ships on and eroded stays a flag — that outcome is acceptable by Q1's own terms and is not a phase failure"), not a collapse: the code stays in the tree behind `?world=eroded` and what stops is spending effort on it. **The one-sentence account is structural, not a defect description: Gate W's instrument set could not see its own product.** Every instrument it had was a proxy — byte-determinism, seam bit-equality, statistics distributions, dispatch timings, and analytic capture shots that BY CONSTRUCTION never render the eroded world. Each was individually sound and each passed honestly; the gate simply had no instrument of the form *look at the thing it makes*, and W-7 — the item that would have added one — was the item held for Gate F. **What Waves 1–3 delivered on the shipping path, traced to mechanism rather than to the sequencing table:** every 6-1 and 6-2-inland term sits inside `if (input.waterInfo.w > 0.0)`, and the w lane has exactly four writers — `appendRiver`/`appendLake` (analytic) push a literal 0, only `appendGraphRiver`/`appendGraphLake` push a payload. So **6-1 is DARK in analytic in BOTH halves** (including lake chop: fetch reaches the shader solely through `appendGraphLake`, so an analytic lake has no fetch and no chop), **6-2 is PARTIAL** (ocean run-up/streaks/wet-sand visible, inland bank run-up dark), and **6-3 is VISIBLE** — ocean-only by construction and driven by `BathymetryClipmap`, which has a first-class analytic path, so `6-3←W-6` was a sequencing dependency on the eroded overlay rather than a data dependency. 6-5 and 6-6 are dark by design (compile-time define; `soilDepthValid` sentinel), 6-7 and 6-8 are visible by design. Wave 1 therefore ships 6-3 whole and 6-2's ocean half; Waves 2–3 ship 6-7 and 6-8 whole. **The three `eroded-*` capture shots were REMOVED rather than skipped**, which is safe only because temporal phase is keyed by canonical INDEX (`findIndex` by name → `simulationTime = 500 + index*120`) and they were the trailing entries, so no surviving shot's index moves — the append-only rule still binds for INSERTION; this is a tail truncation. They could not be left in place because `readBaselinePixels` is called with `required = !REBASELINE`, making a shot with no committed baseline fatal to a normal capture. Verified empirically: the full analytic capture passes at min gray SSIM 0.9982 against the promoted baselines, 119.37 fps minimum, p95 9.60 ms, zero pending pages, 3.5% of the streaming budget, and 492.3 MiB against the 495 ceiling. **Reviver's reading list, in order:** D-9's loosening now outlives its cause and must be re-tightened first (contained — `worstAbsoluteToleranceMeters` in the eroded-only `TerrainPageErosionGpu.ts`, one assertion site, no analytic exposure); D-7's known fix (snap the lattice origin to the 512 m world block, not the evaluating page's own origin); the **analytic-only global height pyramid mixed with eroded page heights** in `PageOcclusionBake.occlusionHeightAt`, which steps from eroded to analytic heights at the page boundary and marches analytic for the remaining ~44 km, reintroducing by data mismatch exactly the discontinuity 4-7's pyramid exists to remove, worst in the deep valleys erosion carves (unmeasured on purpose — the two resolutions are a decision to make once); throughput at ~31 frames/page with one page in flight, so a real session needs ~90 s to build a working set; an admitted-but-unwritten page rendering ZERO HEIGHT rather than the analytic fallback, which is what makes a slow fill look like a broken world; and 527.5 MiB eroded inventoried memory against the 495 ceiling. |
